# 电商业务全自动化系统：项目总纲

- **文档版本：** V1.2
- **状态日期：** 2026-08-14
- **文档状态：** 已确认
- **当前阶段：** 阶段 1，私聊和 `group_sender` 群聊授权文本闭环已验证，媒体与完整恢复待完成

## 总体目标

电商业务横跨微信沟通、文件收集、ERP、平台后台、仓储、物流和报表。本项目建立以 CFserver 为权威控制中心、以 Windows AI 主机为 Agent 执行端的企业自动化系统：员工从原微信会话提交任务，系统先保存消息，再执行身份、权限、上下文和路由控制，由 Hermes 调用获准能力，并将可追踪的结果返回原会话。

生产运行不持续依赖 GitHub 在线。消息、Checkpoint、身份、权限、路由、响应、投递、文件引用、日志和审计必须保留可恢复的权威状态。

## 四层职责

| 层级 | 组件 | 职责 |
| --- | --- | --- |
| 员工入口 | `CF_agent-wechat` | 托管企业 AI 微信客户端，维护登录状态，读取和发送微信消息，并向 Gateway 提供接口；不决定企业身份、权限或 AI 路由 |
| 消息与控制 | `CF_agent-gateway` + PostgreSQL | 负责轮询、Checkpoint、Message Store、身份映射、Admission、Workspace、AI Thread、V2 Routing、Hermes Dispatch、Response Persistence、Delivery Outbox、媒体元数据、日志与审计关联 |
| Agent 执行 | Hermes Gateway | 在 Windows AI 主机上执行 Agent 和模型调用，管理 Hermes 配置档案及后续 Skills；不覆盖 CFserver 权威状态 |
| 文件基础设施 | `CF_filebrowser-enterprise` | 提供企业文件中心、用户权限、Token、分享、WebDAV、审计日志和 AI 文件访问基础设施；正式文件访问必须经 File Service、权限检查和审计 |

## 项目范围

- 微信私聊和获准群聊是第一阶段员工入口，处理结果返回原会话。
- 所有进入 Gateway 的员工消息采用 Persist-first：先持久化，再执行身份、权限和路由判断。
- 未授权消息保留为受控消息历史，但不得调用 Hermes，也不得产生机器人回复。
- 群聊进入 AI 链路必须由发送者明确 `@` 当前机器人；不得从纯文本名称、引用或上一条消息推断 mention。
- 当前企业群聊默认采用 `group_sender` 隔离；`group_shared` 需单独审批和实机验证。
- Windows AI 主机承载 Hermes、后续 Skills 及文档、浏览器和 Windows 自动化能力。
- Hermes 配置档案由 Hermes 自身创建和管理；Gateway 只保存 `external_profile_ref` 并负责选择。
- 正式文件访问统一经过 `CF_filebrowser-enterprise`；不得建设第二套 File Service 或绕过权限与审计。
- 第一阶段不建设独立 OCR；FileBrowser Enterprise 与自动化主线并行推进。
- 寄样、库存、订单、物流、分销链接、商品资料和报表按业务价值分阶段接入。

## 当前生产状态

CFserver 当前承载：

- `CF_agent-wechat` 生产容器。
- Gateway 的 PostgreSQL、`gateway`、`wechat-worker`、`dispatch-worker`、`delivery-worker`，五个服务均保持 healthy。
- Gateway 权威数据库；媒体私有存储与 Attachment/Artifact 桥仍待接入。

Windows AI 主机当前承载 Hermes Gateway 0.20.0。真实私聊和 `group_sender` 群聊文本请求已经完成 Hermes Dispatch、响应持久化和微信投递。Windows 登录启动项存在，但 Hermes 进程仍可能未运行；曾验证 CFserver 到 Hermes 服务端口超时会使 Dispatch 进入 `uncertain` 且没有 AI 回复，人工启动后健康恢复。

`CF_agent-wechat` 使用 `docker/compose.cfserver.yaml`。容器内部运行 Xvfb、fluxbox、dunst、WeChat 与 `agent-server`；生产设置 `ENABLE_VNC=0`，不使用 VNC、noVNC、x11vnc、websockify 或宿主桌面 X11。登录管理脚本与手机确认登录已实机通过，完全新设备 SSH 二维码扫码尚待实机验证。

## 当前验证边界

### 已实机验证

- Gateway 与 `agent-wechat` 的 `cf-internal` 容器网络、Token 鉴权、3 秒常驻轮询、17 个 Checkpoint 和 151 条历史消息基线跳过。
- 新消息 Persist-first、发送者与会话识别、Checkpoint 推进，以及未授权账号不调用 Hermes、不产生回复。
- 私聊授权文本完整链路，包括 `private_sender` 路由、Employee Workspace、AI Thread、Hermes、Response Persistence、Delivery Outbox 和微信实际回复。
- 群聊未明确 `@` 时持久化后以 `bot_not_mentioned` 结束；真正 `@` 时完成 `group_sender` 授权文本闭环并实际回复。
- 同一员工 Workspace 复用；私聊与群聊的 AI Thread 和 Hermes Thread 隔离；同一发送者在同一群内复用 `group_sender` 上下文。
- 机器人回复不回环。
- CFserver Gateway 应用服务 restart 后恢复持久化状态，复用原 AI Thread 和 Hermes 会话上下文。
- 微信引用消息识别、`reply_context` 持久化和引用类型消息文本回复；群聊引用但未真实 `@` 时安全拒绝。
- 微信图片消息识别、Raw Payload 与 Message Store 持久化、媒体 API 提取真实 JPEG 字节，以及签名、大小和 SHA-256 校验。
- Hermes 故障的一次受控人工恢复：先备份并核对证据，再通过 Guard 允许原 Dispatch 第二次执行成功。

### 尚待验证或实现

- `group_shared` 策略。
- 将 `reply_context` 中的被引用内容自动注入 Hermes。
- Attachment 元数据、Gateway 私有入站媒体存储、生命周期和清理策略。
- Hermes 入站媒体协议、多模态理解、出站 `artifact_ref` 下载与 Gateway READY Artifact 物化。
- 图片和文件经 Delivery Outbox、`send_media` 返回微信。
- 完全新设备 SSH 二维码扫码。
- Gateway 容器 recreate、PostgreSQL 重启、CFserver 整机重启、AI 主机重启及 Hermes 自动恢复。
- Hermes 开机自启、守护、健康告警，以及 `uncertain` Dispatch 的正式管理与恢复命令/API。
- FileBrowser 与企业资料接入、Skills 和业务自动化。

统一结论为：

> 私聊和 group_sender 群聊的授权文本闭环已实机验证；媒体链路、引用上下文注入和完整宿主恢复仍待完成。

## 设备与环境分工

| 环境 | 当前状态 | 职责 |
| --- | --- | --- |
| CFserver | **已部署，部分链路待验证** | 承载 `agent-wechat`、Gateway 五服务、PostgreSQL，以及消息、权限、路由、投递、媒体元数据、日志和审计的权威状态 |
| Windows AI 主机 | **已部署，部分链路待验证** | 承载 Hermes Gateway 0.20.0、Hermes 配置档案、模型与 Agent 能力；后续承载 Skills 和 Windows 侧执行 |
| `CF_filebrowser-enterprise` 运行环境 | **开发中** | 提供唯一正式企业 File Service；本次不改变其既有验证结论 |
| 云服务器 | **规划中** | 后续按需提供加密中转或 HTTPS 入口；隔离边界另行确认 |

## 核心业务数据职责

| 系统 | 目标职责 |
| --- | --- |
| 旺店通 ERP | 电商库存、订单、物流和寄样订单 |
| 平台后台 | 对应电商平台的专属状态和操作 |
| S6 | 财务、线下业务和对账 |
| 旺店通 WMS | 仓库现场执行；前期不是主要自动化对象 |

接口、字段、权限、幂等与数据时效须在对应建设阶段验证。

## 六个建设阶段

1. **阶段 1：** 打通可靠的微信消息、身份权限、Hermes、回复投递与文件基础链路。
2. **阶段 2：** 寄样完整闭环与物流回传。
3. **阶段 3：** 旺店通库存、订单、物流与预警。
4. **阶段 4：** 快手做链接及平台高频操作。
5. **阶段 5：** 拼多多、天猫、京东、抖音、微信小店。
6. **阶段 6：** 报表、S6 查询及旺店通与 S6 对账。

当前阶段的严格执行顺序见[当前状态矩阵](./status/current-status.md#下一阶段顺序)，最新验证证据见[2026-08-14 私聊、群聊及媒体验证记录](./status/2026-08-14-private-group-media-validation.md)。

## 总体规划图

![电商业务全自动化系统总体规划](./图表与附件/电商业务全自动化系统-总规划.png)

[XMind 源文件](./图表与附件/电商业务全自动化系统-总规划.xmind)是早期总蓝图；当其表述与后续需求或[技术决策记录](./05_技术决策记录.md)不一致时，以后者为准。
