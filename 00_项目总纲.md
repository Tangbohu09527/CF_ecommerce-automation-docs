# 电商业务全自动化系统：项目总纲

- **文档版本：** V1.1
- **状态日期：** 2026-08-13
- **文档状态：** 已确认
- **当前阶段：** 阶段 1，微信入口和未授权安全链路完成，授权 AI 闭环待验证

## 总体目标

电商业务横跨微信沟通、文件收集、ERP、平台后台、仓储、物流和报表。本项目建立以 CFserver 为权威控制中心、以 Windows AI 主机为 Agent 执行端的企业自动化系统：员工从原微信会话提交任务，系统先保存消息，再执行身份、权限和路由控制，由 Hermes 调用获准能力，并将可追踪的结果返回原会话。

生产运行不持续依赖 GitHub 在线。消息、Checkpoint、身份、权限、路由、响应、投递、文件引用、日志和审计必须保留可恢复的权威状态。

## 四层职责

| 层级 | 组件 | 职责 |
| --- | --- | --- |
| 员工入口 | `CF_agent-wechat` | 托管企业 AI 微信客户端，维护登录状态，读取和发送微信消息，并向 Gateway 提供接口；不决定企业身份、权限或 AI 路由 |
| 消息与控制 | `CF_agent-gateway` + PostgreSQL | 负责轮询、Checkpoint、Message Store、身份映射、Admission、Workspace、AI Thread、V2 Routing、Hermes Dispatch、Response Persistence、Delivery Outbox、日志与审计关联 |
| Agent 执行 | Hermes Gateway | 在 Windows AI 主机上执行 Agent、模型调用及后续 Skills 和工具调用；不覆盖 CFserver 权威状态 |
| 文件基础设施 | `CF_filebrowser-enterprise` | 提供企业文件中心、用户权限、Token、分享、WebDAV、审计日志和 AI 文件访问基础设施；正式文件访问必须经 File Service、权限检查和审计 |

## 项目范围

- 微信私聊和获准群聊是第一阶段员工入口，处理结果返回原会话。
- 所有进入 Gateway 的员工消息采用 Persist-first：先持久化，再执行身份、权限和路由判断。
- 未授权消息保留为受控消息历史，但不得调用 Hermes，也不得产生机器人回复。
- 群聊进入 AI 链路时必须由发送者明确 `@` 当前机器人；不得从纯文本名称、引用或上一条消息推断 mention。
- Windows AI 主机承载 Hermes、后续 Skills 及文档、浏览器和 Windows 自动化能力。
- 正式文件访问统一经过 `CF_filebrowser-enterprise`；不得建设第二套 File Service 或绕过权限与审计。
- 第一阶段不建设独立 OCR；FileBrowser Enterprise 与自动化主线并行推进。
- 寄样、库存、订单、物流、分销链接、商品资料和报表按业务价值分阶段接入。

## 当前生产状态

CFserver 当前承载：

- `CF_agent-wechat` 生产容器。
- Gateway 的 PostgreSQL、`gateway`、`wechat-worker`、`dispatch-worker`、`delivery-worker`，五个服务均保持 healthy。
- Gateway 权威数据库与受控文件存储。

Windows AI 主机当前承载 Hermes Gateway 0.20.0。CFserver 与 `dispatch-worker` 到 Hermes 的网络连通已验证；Windows 登录启动项存在，但主机重启后 Hermes 没有可靠自动启动，人工启动后恢复。

`CF_agent-wechat` 使用 `docker/compose.cfserver.yaml`。容器内部运行 Xvfb、fluxbox、dunst、WeChat 与 `agent-server`；生产设置 `ENABLE_VNC=0`，不使用 VNC、noVNC、x11vnc、websockify 或宿主桌面 X11。登录管理脚本与手机确认登录已实机通过，完全新设备 SSH 二维码扫码尚待实机验证。

## 当前验证边界

### 已实机验证

- Gateway 与 `agent-wechat` 通过 `cf-internal` 容器网络通信，Token 鉴权有效。
- 微信每 3 秒常驻轮询。
- 17 个现有聊天建立 Checkpoint。
- 以 `bootstrap_mode=latest` 将 151 条历史消息作为基线跳过。
- 新私聊消息进入 Message Store，发送者与会话识别正确，Checkpoint 推进。
- 未授权账号被安全拒绝，未调用 Hermes，未产生机器人回复。
- CFserver 与 `dispatch-worker` 可访问 Hermes Gateway 0.20.0。

### 尚待验证

- 测试发送者的 Enterprise Identity、Source Identity Mapping、User Access Policy 和 Gateway Access Policy。
- Agent Profile、私聊 Conversation-AgentProfile Binding、Admission Allowed 与 V2 Routing。
- Hermes 实际处理测试消息、Response Persistence、Delivery Outbox 与微信收到真实回复。
- Hermes Gateway 开机自启可靠性。
- 完全新设备 SSH 二维码扫码、群聊 `@` 机器人、图片、文件和引用消息。
- FileBrowser 与企业资料接入、Skills 和业务自动化。

因此当前不是“全部完成”。统一结论为：

> 微信消息发现、持久化、Checkpoint、未授权拒绝和 Hermes 网络连通已实机验证；授权后的完整 AI 回复闭环仍待验证。

较早的 V1 Staging 授权文本闭环只作为其特定环境的历史证据，不替代当前 CFserver 生产验收。

## 设备与环境分工

| 环境 | 当前状态 | 职责 |
| --- | --- | --- |
| CFserver | **已部署，部分链路待验证** | 承载 `agent-wechat`、Gateway 五服务、PostgreSQL、文件存储及消息、权限、路由、投递、日志和审计的权威状态 |
| Windows AI 主机 | **已部署，部分链路待验证** | 承载 Hermes Gateway 0.20.0、模型与 Agent 能力；后续承载 Skills 和 Windows 侧执行 |
| `CF_filebrowser-enterprise` 运行环境 | 以该项目现有状态为准 | 提供唯一正式企业 File Service；本次不改变其具体状态 |
| 云服务器 | **规划中** | 后续按需提供加密中转或 HTTPS 入口；隔离边界另行确认 |

## 核心业务数据职责

| 系统 | 目标职责 |
| --- | --- |
| 旺店通 ERP | 电商库存、订单、物流和寄样订单 |
| 平台后台 | 对应电商平台的专属状态和操作 |
| S6 | 财务、线下业务和对账 |
| 旺店通 WMS | 仓库现场执行；前期不是主要自动化对象 |

接口、字段、权限、幂等与数据时效须在对应建设阶段验证。

## 六个建设阶段

1. **阶段 1：** 打通可靠的微信消息、身份权限、Hermes、回复投递与文件基础链路。
2. **阶段 2：** 寄样完整闭环与物流回传。
3. **阶段 3：** 旺店通库存、订单、物流与预警。
4. **阶段 4：** 快手做链接及平台高频操作。
5. **阶段 5：** 拼多多、天猫、京东、抖音、微信小店。
6. **阶段 6：** 报表、S6 查询及旺店通与 S6 对账。

当前阶段的严格执行顺序见[当前状态矩阵](./status/current-status.md#下一阶段顺序)，当天证据和阻塞项见[微信运行时阶段收口记录](./status/2026-08-13-wechat-runtime-closeout.md)。

## 总体规划图

![电商业务全自动化系统总体规划](./图表与附件/电商业务全自动化系统-总规划.png)

[XMind 源文件](./图表与附件/电商业务全自动化系统-总规划.xmind) 是早期总蓝图；当其表述与后续需求或[技术决策记录](./05_技术决策记录.md)不一致时，以后者为准。
