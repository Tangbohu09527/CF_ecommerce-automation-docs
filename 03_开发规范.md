# 开发规范

## 仓库与命名

- **已确定：** 所有相关 GitHub 仓库统一使用 `CF_` 前缀；当前文档仓库固定为 `CF_ecommerce-automation-docs`。
- 仓库名建议采用 `CF_<业务域>-<职责>`，一个仓库只承担清楚的交付边界。
- **后续规划示例：** `CF_ecommerce-automation-core`、`CF_ecommerce-automation-wechat`、`CF_ecommerce-automation-skills`；仅在边界明确后创建，示例不是创建决定。
- **已确定：** FileBrowser Enterprise 仓库现名为 `CF_filebrowser-enterprise`，作为正式企业 File Service 与自动化主线并行开发；本仓库任务不得修改其实现。
- 不为形式完整创建大量空目录、空文档、许可证、Pages、Actions 或未使用的代码骨架。

## Git 简化规则

- 默认分支使用 `main` 并保持可审阅；功能和修复使用短期分支，如 `feat/issue-12-batch`、`fix/issue-18-retry`、`docs/issue-3-context`。
- 提交应小而完整，推荐 `<type>: <中文摘要>`，常用类型为 `docs`、`feat`、`fix`、`test`、`refactor`、`chore`。
- 提交前检查差异、测试、文档和敏感信息；不得把本地环境文件或调试业务数据一并提交。
- Tag 只标记可复现、已说明变更的版本，建议使用语义化版本 `vMAJOR.MINOR.PATCH`；未形成发布物时不滥用 Tag。
- 不直接改写共享分支历史；合并方式和保护规则待各代码仓库建立后确认。

## 文档规则

- 正文使用简洁中文 Markdown，技术专名、标识符和代码可保留英文。
- 使用“已确定”“待技术验证 / 待确认”“后续规划”区分结论，不把讨论过程、建议或测试计划写成已完成功能。
- 文档按 [AGENTS.md](./AGENTS.md#文档更新归属) 的归属更新；相同内容只完整记录一次，其他位置使用相对链接。
- 需求、设计、部署和技术决定随代码与正式决定同步更新；过期结论应被替换或在决策记录中标明已取代。
- Issue 保留排障过程、实验数据和讨论；正式文档只保留当前正确结论及必要依据。
- 检查 UTF-8 编码、标题层级、相对链接、图片路径和明显错别字。

## 配置、密钥与数据

- 密钥、密码、Token、Cookie、微信登录数据、生产附件和真实业务数据不得提交。
- 本地配置使用 `.env` 且必须被忽略；可提交的 `.env.example` 只包含变量名、无敏感默认值和简短说明。
- 日志、截图、测试夹具和错误样本必须脱敏；确需保存敏感诊断材料时使用仓库外的受控存储。
- 凭证从环境或正式密钥服务注入，代码与文档不得提供真实值。

## 数据库与接口变更

- 所有数据库结构变更必须通过有序 migration，包含升级路径；破坏性变更还要给出备份、兼容期和回滚方案。
- 接口和事件结构使用版本化 schema；新增字段优先保持向后兼容，生产者与消费者按明确顺序升级。
- 数据口径、唯一键、幂等键和时区必须在相应设计或 migration 中说明，不依赖口头约定。

## 质量与发布

- 修复 Bug 时优先增加能稳定复现问题的自动化测试，再验证修复和相关回归；无法自动化时在 Issue 记录可重复的人工步骤。
- 核心链路至少覆盖正常、重复、超时、离线恢复、权限拒绝和部分失败场景。
- 正式环境不直接编辑代码或手工修改数据库；变更必须来自可追踪版本、经过检查并能回滚。
- 部署前核对配置差异、migration、依赖、备份和回滚条件，部署后执行健康检查。

## Skill 维护

- Skill 说明保持简短，和实现就近放在承担该 Skill 的代码仓库，不集中复制到本文档仓库。
- 每个 Skill 应说明输入、输出、所需权限、幂等性、失败分类和人工确认点。
- Skill 不得绕过任务中心、File Service、权限检查和审计，也不得自行扩大可访问目录或账号范围。
