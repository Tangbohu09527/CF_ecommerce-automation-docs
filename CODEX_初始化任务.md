# Codex任务：初始化 CF_ecommerce-automation-docs 私密文档仓库

## 一、任务目标

在当前目录创建“电商业务全自动化系统”的独立文档仓库第一版，并在检查无误后创建 GitHub 私密仓库：

`CF_ecommerce-automation-docs`

本任务只允许操作当前目录。不得读取、修改、提交或重命名其他仓库，尤其不得触碰仍在运行任务的 `filebrowser-enterprise` 项目。

## 二、先做环境检查

1. 确认当前目录名称为 `CF_ecommerce-automation-docs`。
2. 确认当前目录是独立 Git 仓库；若尚未初始化，执行 `git init -b main`。
3. 检查 `git status`，确保没有指向其他项目的远程仓库。
4. 检查 `gh auth status`。如果 GitHub CLI 未登录，不要尝试绕过，提示用户先完成 `gh auth login`。
5. 检查以下附件是否存在：
   - `图表与附件/电商业务全自动化系统-总规划.xmind`
   - `图表与附件/电商业务全自动化系统-总规划.png`
6. 阅读 PNG 总体规划图；如有必要，将 XMind 当作 ZIP 解包到临时目录读取其结构化内容，但不要修改原始 XMind。

## 三、创建的仓库结构

请创建并填写以下文件：

```text
CF_ecommerce-automation-docs/
├── README.md
├── AGENTS.md
├── .gitignore
├── 00_项目总纲.md
├── 01_功能需求.md
├── 02_系统设计.md
├── 03_开发规范.md
├── 04_部署运维.md
├── 05_技术决策记录.md
├── CODEX_初始化任务.md
└── 图表与附件/
    ├── 电商业务全自动化系统-总规划.xmind
    └── 电商业务全自动化系统-总规划.png
```

不要额外创建大量空目录、空文档、许可证文件、GitHub Pages、Actions 工作流或代码目录。

## 四、当前已确定、不得擅自改变的项目事实

所有文档必须以以下决定为当前有效基线：

1. 整个项目名称为“电商业务全自动化系统”。
2. 所有相关 GitHub 仓库统一使用 `CF_` 前缀。
3. 本文档仓库名称固定为 `CF_ecommerce-automation-docs`。
4. 当前 `filebrowser-enterprise` 仓库暂不重命名；等其正在运行的 Codex 任务结束后，再另行处理。
5. 生产 Agent 框架固定使用 Hermes，不使用 OpenClaw。
6. 模型计划使用 GPT-5.6 API。
7. 第一阶段不单独建设 OCR 系统；图片和扫描件能力以后按实际需要补充。
8. 微信接入候选已经明确为 `agent-wechat`，计划部署在 Debian 服务器。
9. `agent-wechat` 只负责微信收发及附件获取，不负责权威上下文、权限、任务状态或业务执行。
10. Debian 是消息、上下文、任务、文件、权限、日志和审计的权威控制中心。
11. 新 Windows AI 电脑负责 Hermes、Worker Bridge、GPT-5.6 调用、Skills、文档表格处理、浏览器及 Windows 自动化。
12. 当前电脑继续并行二次开发 FileBrowser Enterprise。
13. 群聊 AI 线程按“微信机器人账号 + 群 chat_id + 发信人 sender_id”区分；私聊按“微信机器人账号 + 私聊 chat_id”区分。
14. 发给 Hermes 的上下文应优先包含：当前消息、文字引用、当前任务附件、同一发信人的最近消息、少量必要群聊上下文、未完成关联任务状态。
15. 机器人无需发送微信原生引用回复，但接收侧需要读取可获得的文字引用上下文。
16. 系统计划接收图片、Word、Excel、PowerPoint、PDF、ZIP及其他常见压缩包；附件既可能是任务输入，也可能只需要保存到文件系统指定逻辑路径。
17. 多条连续消息和多个附件需要通过“任务批次”合并，不能每条消息都机械创建独立任务。
18. AI 主机离线、Hermes异常或GPT接口超时时，Debian中的消息、附件和任务不得丢失。
19. Hermes不能任意读写整个正式存储目录；正式文件操作应经过 Debian File Service、权限检查和审计。
20. FileBrowser Enterprise与自动化主线并行推进；自动化业务不等待FileBrowser二开全部完成。
21. 业务跑通前，代码和文档依赖GitHub私密仓库进行中央版本管理；生产运行本身不应依赖GitHub持续在线。

## 五、每份文档的要求

### 1. `README.md`

控制在约一页，包含：

- 仓库用途；
- 项目一句话目标；
- 当前固定技术方案；
- 文档入口；
- 相关仓库清单；
- 当前状态。

不要复制其他文档的大段内容。

### 2. `AGENTS.md`

这是给后续 Codex 和其他代码AI读取的固定入口，使用中文，内容简洁明确，至少包括：

- 阅读顺序；
- 当前固定技术决定；
- 文档更新归属；
- 不得重复记录同一信息；
- 不得把讨论过程当成最终决定；
- 不得擅自引入OpenClaw；
- 不得修改其他仓库；
- 不得提交密钥、密码、Cookie、微信登录数据或真实业务文件。

### 3. `00_项目总纲.md`

依据XMind和当前有效决定，写明：

- 项目背景与目标；
- 项目范围和暂不包含内容；
- 十个总体规划模块；
- Debian、当前开发电脑、新Windows AI电脑、云服务器的分工；
- 六个建设阶段；
- 当前阶段；
- 嵌入总体规划PNG图片；
- 说明XMind是总蓝图，后续明确决定优先于图中的早期表述。

### 4. `01_功能需求.md`

只写“系统要做什么”，不要写具体代码实现。按以下模块组织：

- 微信私聊和群聊；
- 发信人和允许群识别；
- 上下文与文字引用；
- 图片、Word、Excel、PPT、PDF、ZIP接收；
- 多消息与多附件任务批次；
- 附件作为任务输入；
- 文件直接保存和处理后归档；
- Hermes处理及Skill调用；
- 结果回传原聊天窗口；
- 权限、确认、失败提示；
- 后续寄样、库存、订单、物流、做链接、报表等业务方向。

每项需求尽量采用“场景、系统行为、异常处理、验收标准”的短结构。对尚未验证的 `agent-wechat` 文件兼容性，明确标记为“待技术验证”，不要写成已经稳定支持。

### 5. `02_系统设计.md`

写“系统准备怎么实现”，至少包括：

- 总体架构和主链路；
- `agent-wechat`、`wechat-relay`、上下文服务、任务中心、文件服务、Hermes Worker Bridge、Hermes、Skills的职责边界；
- 物理微信会话与AI线程的区别；
- 上下文选择优先级和上下文快照；
- 多消息、多附件任务批次；
- 附件下载、临时存储、哈希、任务关联、处理、结果回传和正式归档；
- Debian与Windows AI电脑之间通过内网API传递任务和文件，不使用Base64承载大文件；
- 任务状态机、幂等、重试和离线恢复；
- 初步核心数据表清单；
- Hermes结构化输入输出原则；
- 文件与权限安全边界；
- FileBrowser Enterprise、File Service与真实文件系统之间的关系。

对尚未最终选定的编程语言、队列产品、数据库细节等，使用“待定/建议”，不得自行假定已经拍板。

### 6. `03_开发规范.md`

至少包括：

- 所有项目仓库统一使用 `CF_` 前缀；
- 仓库建议命名；
- Git分支、提交和Tag的简化规则；
- 文档用中文Markdown；
- 密钥和生产数据不得提交；
- `.env`与`.env.example`规则；
- 数据库变更需要migration；
- 修复Bug优先增加复现测试；
- 正式环境不直接改代码；
- 文档随代码和决定同步更新；
- Issue记录排障过程，正式文档只保留当前正确结论；
- Skill说明保持简短，就近放在相应代码仓库。

### 7. `04_部署运维.md`

目前只建立实用骨架，不虚构尚未部署的命令。至少写：

- 计划中的Debian服务清单；
- 计划中的Windows AI节点服务清单；
- 部署前检查项；
- 健康检查项；
- 微信收到但没有回复的排查链路；
- Hermes Worker离线时的处理原则；
- 备份、升级、回滚的原则；
- 日志和trace_id/task_id定位原则；
- 尚未部署的内容明确标记“待实施”。

### 8. `05_技术决策记录.md`

采用简短编号记录，每个决定包含“日期、状态、决定、原因、影响”。至少写入：

- D001：生产Agent使用Hermes，不使用OpenClaw；
- D002：模型使用GPT-5.6 API；
- D003：微信接入使用agent-wechat；
- D004：agent-wechat计划部署在Debian；
- D005：Debian是消息、上下文、任务和文件的权威来源；
- D006：第一阶段不建设独立OCR；
- D007：FileBrowser与自动化主线并行开发；
- D008：所有相关仓库使用CF_前缀；
- D009：业务跑通前使用GitHub私密仓库作为中央版本；
- D010：当前filebrowser-enterprise仓库暂不重命名。

## 六、写作规则

1. 全部正文使用中文；技术专有名称可保留英文。
2. 内容要精简、可读，不要为了“正规”写成冗长模板。
3. 不重复粘贴相同架构描述；使用交叉引用。
4. 明确区分“已确定”“待技术验证”“后续规划”。
5. 不虚构已经完成的部署、测试、接口、表结构或软件版本。
6. 不把本任务说明里的句子机械原样复制到所有文档，应整理成自然、统一、可维护的文档。
7. Markdown链接和图片路径必须可用。
8. 检查文件编码为UTF-8，检查无明显错别字和断链。

## 七、GitHub任务清单

在仓库中创建以下 GitHub Issues，使用中文标题和正文，并添加适当标签；如果标签不存在，可创建 `文档`、`需求`、`架构`、`技术验证`、`待确认`：

1. `确认项目总纲第一版`  
   对XMind与00_项目总纲进行人工核对，确认范围、设备分工和六阶段顺序。

2. `确认微信机器人V1功能与验收标准`  
   重点确认群聊/私聊、发信人线程、上下文、引用文字、多附件、结果回传。

3. `执行agent-wechat附件兼容性技术验证`  
   测试普通图片、中文文件名Word、Excel、PPT、PDF、ZIP，群聊与私聊，连续多附件，记录实际结果。

4. `确认Debian与Windows AI节点部署边界`  
   最终确认服务部署位置、内网通信、文件传输、端口、安全和备份边界。

5. `设计V1消息、上下文、任务和附件数据模型`  
   在技术验证后确定最小表结构、唯一键、任务状态和上下文快照字段。

不要创建大量远期Issues。

## 八、检查、提交和创建私密仓库

完成文件后：

1. 运行内容检查：列出文件、检查Markdown引用、检查Git状态。
2. 输出一份简短总结，说明创建了什么、哪些内容标为待验证。
3. 执行：

```bash
git add .
git commit -m "docs: 建立电商业务全自动化系统文档仓库"
```

4. 如果 `origin` 不存在且 `gh auth status` 正常，创建GitHub私密仓库并推送：

```bash
gh repo create CF_ecommerce-automation-docs \
  --private \
  --description "电商业务全自动化系统的项目总纲、需求、系统设计、开发规范与部署运维文档" \
  --source . \
  --remote origin \
  --push
```

在 Windows PowerShell 中可以将上面的多行命令改为单行执行。

5. 创建第七部分列出的5个GitHub Issues。
6. 最后再次执行：

```bash
git status
git remote -v
gh repo view --web
```

如果任何网络、登录或权限步骤失败，不得删除本地成果；保留本地提交并清楚说明需要用户完成的操作。

## 九、最终回报格式

完成后只需汇报：

- 本地仓库路径；
- GitHub私密仓库地址；
- 首次提交哈希；
- 已创建的文档清单；
- 已创建的Issue清单；
- 标记为“待技术验证/待确认”的重点；
- 是否有任何步骤需要用户继续操作。
