# 技术决策记录

本文件是已经形成的技术决定正文、状态和取代关系的唯一权威来源，并保留被新决定取代所需的最小历史。主题导航见[ADR 索引](./adr/README.md)；该索引不复制或取代本文件。状态列统一使用仓库状态语义；历史行的“已确定”只表示该决定当时曾正式形成，是否仍为当前基线以“决定”栏指向的后续记录为准。若决定变化，应新增记录，不要用讨论过程覆盖历史。

| 编号 | 日期 | 状态 | 决定 | 原因 | 影响 |
| --- | --- | --- | --- | --- | --- |
| D001 | 2026-07-19 | 已确定 | 生产 Agent 使用 Hermes，不使用 OpenClaw。 | 统一生产 Agent 路线，避免双框架造成维护和权限边界分裂。 | 设计、部署和 Skills 均围绕 Hermes；不得擅自引入 OpenClaw。 |
| D002 | 2026-07-19 | 已确定 | 模型计划使用 GPT-5.6 API。 | 为生产 Agent 确立统一模型接口基线。 | Worker 需实现超时、限流、错误处理和受控密钥注入；实际接入仍待实施。 |
| D003 | 2026-07-19 | 已确定；能力待技术验证 | 微信接入使用 `agent-wechat`。 | 已明确微信接入候选，便于集中验证和设计适配层。 | 通过适配边界接入；图片、Office、PDF、ZIP、中文文件名及多附件兼容性必须实测。 |
| D004 | 2026-07-19 | 已确定 | `agent-wechat` 计划部署在 Debian。 | 微信入口应靠近权威消息与任务控制面，并与 Windows 执行节点解耦。 | Debian 负责其运行与恢复；它只做微信收发和附件获取，不承载权威业务状态。 |
| D005 | 2026-07-19 | 已确定 | Debian 是消息、上下文、任务、文件、权限、日志和审计的权威来源。 | AI 节点可能离线或更换，核心状态必须可靠持久化并可恢复。 | Windows Worker 无权以本地状态覆盖 Debian；离线期间消息、附件和任务不得丢失。 |
| D006 | 2026-07-19 | 已确定 | 第一阶段不建设独立 OCR。 | 先验证主链路和高价值业务，避免提前建设未证实必要的子系统。 | 图片/扫描件能力按实际需求补充；不得把 XMind 中的 OCR 设想写成阶段 1 已建能力。 |
| D007 | 2026-07-19 | 已确定 | FileBrowser Enterprise 与自动化主线并行开发。 | 文件管理二次开发不应阻塞自动化业务闭环，反之亦然。 | 两条工作流独立推进，通过 `CF_filebrowser-enterprise` 提供的稳定 File Service 契约衔接；本仓库不得修改另一仓库。 |
| D008 | 2026-07-19 | 已确定 | 所有相关 GitHub 仓库使用 `CF_` 前缀。 | 保持项目资产可识别、命名一致。 | 新仓库创建前须核对前缀；FileBrowser Enterprise 仓库现名为 `CF_filebrowser-enterprise`。 |
| D009 | 2026-07-19 | 已确定 | 业务跑通前使用 GitHub 私密仓库作为中央版本管理。 | 在早期提供集中、可追踪和可恢复的协作基线，同时控制文档与代码可见性。 | 仓库必须为 PRIVATE；不得提交敏感数据；生产运行不依赖 GitHub 持续在线。 |
| D010 | 2026-07-19 | 已确定 | 历史记录：`filebrowser-enterprise` 仓库曾暂不重命名；当前已由 D012 取代。 | 当时该仓库有正在运行的 Codex 任务，改名会造成干扰和风险。 | 临时命名约束已经结束；当前仓库名和职责边界以 D012 为准。 |
| D011 | 2026-08-01 | 已确定 | `enterprise_identity_id` 是 Gateway 内部不可变企业身份主键，也是身份、工作区和权限关联的权威主键；`employee_id` 仅为可空业务编号。 | 来源账号、公司编号和运行时工作区具有不同生命周期，混用会造成身份合并、权限漂移和数据关联不稳定。 | Identity Mapping 只输出 `enterprise_identity_id` 和可选 `employee_id`，不创建 `workspace_id`；只有身份映射成功且 Access Control 允许创建 Task 后，Employee Conversation Manager 才解析或创建工作区和 AI Thread。 |
| D012 | 2026-08-03 | 已确定 | `CF_filebrowser-enterprise` 是正式企业 File Service；FileBridge / `filebrowser-agentctl` 是受控客户端，不建设第二套 File Service。 | 统一正式文件访问的身份、路径、权限、capability 和 Audit 边界，避免人员入口与自动化入口形成两套授权规则。 | Gateway、Hermes、Skills 和受控客户端只能调用 File Service API，不得绕过文件权限、Token capability、Share capability 或 Audit；V1 Beta 完成后，Gateway/Hermes 只按稳定 File Service 契约集成。 |
| D013 | 2026-08-03 | 已确定 | API Token 只存储 hash，明文只在创建响应中展示一次；Share 的 `configured` 是配置值，`effective` 是服务端计算的实际生效 capability；Audit 基础设施完成不等于所有业务 Audit Action 已完成。 | 防止凭证恢复、客户端自行放大权限或以局部审计能力替代完整业务审计。 | 明文 Token 仅可在创建响应中读取并一次性展示，之后不得再次查询、恢复或持久化；客户端不得用 `configured` 推导实际授权，未覆盖的业务 Audit Action 必须保持待集成状态。 |
| D014 | 2026-08-04 | 已确定 | Share 使用 `configuredCapabilities` / `effectiveCapabilities` 分层；九项 capability 保留显式布尔值，Browse / Preview 按服务端规则派生，Upload Share 强制 Create。 | 配置意图、用户权限和最终授权不能混为一层，`false` 丢失或客户端自行推导会造成越权。 | 客户端只执行服务端返回的 effective capability；普通用户和管理员均不得从 configured 值扩权，序列化不得省略 `false`。 |
| D015 | 2026-08-04 | 已确定 | Share 密码更新采用保持、移除、替换三态，缺省或 `null` 均表示保持；Share credential 与 active hash 精确绑定。 | 避免编辑 Share 时意外清除密码，并阻止密码、Token 或 Cookie 跨 Share、空 hash 或错误 hash 发送。 | POST / PATCH 更新清理 legacy `Link.Token`；切换、退出、加载失败和卸载时清理运行时密码，同一 Share 正常导航可保留；hash 不匹配时不发送凭证。 |
| D016 | 2026-08-04 | 已确定 | Persistent Audit 是 File Service 的安全事实来源；Event 与 SSE 只能通知，不能替代持久化审计；每个外部请求最多形成一个最终 Audit Event。 | 瞬时事件无法提供恢复、查询和安全追责所需的持久事实，重复最终事件会造成审计结论冲突。 | Audit 使用 RequestID、request-scoped Recorder、签名 Cursor 和 Recovery；日志、事件流或管理 UI 不得被当作权威审计存储。 |
| D017 | 2026-08-04 | 已确定 | 高风险写操作使用 durable pending，并在首个业务副作用前完成 `ReservePending`。 | 审计 reservation 失败后仍发生写入会形成不可追踪副作用，Finalize 失败后丢弃 Pending 会破坏恢复能力。 | reservation 失败返回 503 且业务副作用为零；Finalize 失败保留 Pending，由 Recovery 收口。 |
| D018 | 2026-08-04 | 已确定 | 自动化客户端使用普通用户和最小权限 API Token，只能通过唯一正式 File Service 访问正式文件。 | 管理员身份或直连存储会绕过用户权限、Token / Share capability 与 Persistent Audit，形成第二套实际授权路径。 | FileBridge / `filebrowser-agentctl`、Gateway、Hermes 和 Skills 不得直接访问正式存储，不得建设第二套 File Service，也不得把管理员凭证作为常规自动化身份。 |
| D019 | 2026-08-13 | 已确定 | `agent-wechat` 生产环境不使用 VNC、noVNC、x11vnc 或 websockify，也不使用宿主桌面 X11；设置 `ENABLE_VNC=0`。 | 自动化依赖 AT-SPI 与容器内部图形环境，日常登录由脚本管理，不需要浏览器远程桌面；禁用额外远程桌面可减少暴露面和运维复杂度。 | 容器内部继续运行 Xvfb、fluxbox、dunst、WeChat 与 `agent-server`；登录和故障处理以受控脚本为主。 |
| D020 | 2026-08-13 | 已确定 | Gateway 与 `agent-wechat` 使用 `cf-internal` 容器网络。 | 让入口与控制面通过受控内部网络通信，避免依赖宿主桌面或非必要外部暴露。 | 内网连接仍必须执行 Token 鉴权；网络可达不代表获得业务权限。 |
| D021 | 2026-08-13 | 已确定 | 微信员工消息采用 Persist-first：先写 Message Store，再执行身份与权限判断。 | 未授权消息仍是企业消息历史；若先判断或执行，可能丢失审计事实或产生不可恢复副作用。 | 持久化失败不得推进执行链；Admission 拒绝只阻止 AI 工作，不删除已保存消息。 |
| D022 | 2026-08-13 | 已确定 | 微信轮询、AI 派发和结果投递分别由 `wechat-worker`、`dispatch-worker`、`delivery-worker` 承担。 | 三类工作具有不同依赖、重试和扩缩容边界。 | Gateway 不是单一常驻进程；每个 Worker 独立健康检查，执行成功与投递成功分开记录。 |
| D023 | 2026-08-13 | 已确定 | 微信运行时首次启用采用 `bootstrap_mode=latest`。 | 避免把启用前的大量历史消息重新解释为新任务。 | 首次建立当前高水位；历史基线不进入 AI，之后只处理新增消息。 |
| D024 | 2026-08-13 | 已确定 | Token、API Key 和数据库密码不得写入普通 YAML。 | 普通配置文件容易进入版本控制、日志、备份或协作附件。 | 凭证只通过 root-only 文件、未提交的 `.env` 或受控密钥系统注入；文档仅使用占位符。 |
| D025 | 2026-08-13 | 已确定 | 身份、两级访问策略、Agent Profile、会话绑定及其后续链路完成前，不得宣称完整 AI 回复闭环已验证。 | 服务 healthy、Hermes 网络可达和未授权拒绝都不能证明获准消息已完成执行与投递。 | 只有 Admission Allowed、V2 Routing、Hermes Dispatch、Response Persistence、Delivery Outbox 和微信真实回复逐项通过后，才可更新闭环状态。 |
| D026 | 2026-08-14 | 已确定 | Hermes 配置档案由 Hermes 创建和管理；Gateway 只保存并选择 `external_profile_ref`。 | AI 人格、运行配置、技能和 `SOUL.md` 属于 Hermes 执行域，Gateway 需要稳定引用而不应复制内部配置。 | Conversation 与 Group Type 通过 Agent Profile 选择外部档案；档案生命周期由 Hermes 管理，Gateway 不自动创建。 |
| D027 | 2026-08-14 | 已确定 | 入站媒体在提交 Hermes 前必须经过 Gateway 受控持久化。 | 仅在内存中转发无法提供恢复、完整性、生命周期和审计保证。 | 媒体经 MIME、签名、大小、文件名和 SHA-256 校验后进入私有存储并创建 Attachment；未就绪或未获准媒体不得进入 AI。 |
| D028 | 2026-08-14 | 已确定 | 出站媒体必须先物化为 Gateway `READY` Artifact，再进入微信投递。 | Hermes 返回外部引用不等于文件已被 Gateway 安全取得，直接投递会产生丢失、重复或不完整文件。 | Gateway 受认证下载 `artifact_ref`，经 ArtifactRepository 原子持久化和完整性验证后才能创建 Delivery Outbox。 |
| D029 | 2026-08-14 | 已确定 | `uncertain` Dispatch 不自动盲重试，必须通过证据核对、幂等 Guard 和正式管理流程恢复。 | 超时无法证明外部执行没有发生，盲重试可能造成重复回复或业务副作用。 | 建设受控命令/API与审计；人工直接修改数据库不得成为常规运维方式。 |
| D030 | 2026-08-14 | 已确定 | `group_sender` 是当前企业群聊默认隔离策略；`group_shared` 必须单独审批和实机验证。 | 同一物理群聊不等于成员可以共享 AI 上下文；默认按发送者隔离可降低越权和串线风险。 | 群聊默认按群与发送者建立线程；启用共享前需明确共享主体、权限、数据边界、迁移和回滚。 |
| D031 | 2026-08-14 | 已确定 | CFserver Gateway 应用服务 restart 验证不能替代容器 recreate、PostgreSQL 重启、CFserver 整机重启或 AI 主机重启验收。 | 不同恢复层级涉及容器生命周期、数据库持久化、网络、登录态和 Hermes 自启，风险边界不同。 | 每个层级单独演练和留证；不得用较低层级结果宣称完整宿主恢复完成。 |

最新阶段证据见[2026-08-14 私聊、群聊及媒体验证记录](./status/2026-08-14-private-group-media-validation.md)；上一阶段证据见[2026-08-13 微信运行时收口记录](./status/2026-08-13-wechat-runtime-closeout.md)。
