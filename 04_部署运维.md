# 部署运维

> 当前状态：阶段 1 基础测试运行环境已于 2026-07-22 完成；截至 2026-07-29，`agent-wechat` 部署及微信登录、联系人/聊天/消息读取和消息发送已完成基础验证。截至 2026-08-03，`CF_filebrowser-enterprise` V1 Beta 核心服务端能力已实现，但最终 Debian 部署仍待验证。下文按组件分别标记状态，不因代码实现或入口能力验证完成而视为生产上线或端到端主链路完成。

## 测试运行环境基线（2026-07-22）

### 节点与网络

| 节点 | 系统与名称 | 网络配置 | 当前用途 |
| --- | --- | --- | --- |
| Windows AI 开发节点 | Windows 11；计算机名 `AItest` | `192.168.1.232/24` | Codex 开发、Git/GitHub 开发环境和 v2rayN 局域网代理出口；Hermes 运行节点为**后续规划**。 |
| Debian 测试节点 | Hyper-V 虚拟机名 `CF_wechat-lab`；主机名 `cf-wechat-lab`；Debian 13 Trixie | `eth0` 使用固定地址 `192.168.1.230`；通过 SSH 以用户 `linxi` 管理 | 承载阶段 1 的 Debian 侧服务验证；`agent-wechat` 部署和基础微信能力已验证，其他控制面服务待实施。 |
| 正式存储服务器 | `CFserver` | `192.168.1.233` | 正式存储节点；当前只记录网络位置，不表示测试环境已经完成正式文件服务对接。 |

Debian 测试节点的内核版本为 `6.12.96+deb13-amd64`。Windows AI 节点与 Debian 测试节点处于同一 `192.168.1.0/24` 局域网，后续通过内网接口验证 Debian 控制面与 Windows Hermes Worker 的协作。

### Hyper-V 关系

- `CF_wechat-lab` 运行在 Windows AI 节点 `AItest` 的 Hyper-V 中，使用 External Switch 网络模式。
- External Switch 使虚拟机以独立地址直接接入局域网，便于 SSH 管理，并与 Windows AI 节点及后续内网服务按真实节点关系联调。
- Windows 节点提供虚拟化宿主和 v2rayN 代理出口；Debian 虚拟机保持独立的操作系统、地址和服务边界。该宿主关系不改变 Debian 控制面与 Windows 执行节点的既定职责划分。

### Docker 环境

| 项目 | 已验证配置或结果 | 状态 |
| --- | --- | --- |
| Docker Engine | `Docker version 26.1.5+dfsg1, build a72d7cd` | 安装完成 |
| Docker Compose | `Docker Compose version 2.26.1-4` | 安装完成 |
| Docker daemon 代理 | HTTP、HTTPS 代理指向 Windows AI 节点的 v2rayN；本机及局域网地址直连 | 配置完成 |
| 容器运行验证 | `hello-world` | 测试通过 |

Docker daemon 的代理环境为：

```ini
HTTP_PROXY=http://192.168.1.232:10808
HTTPS_PROXY=http://192.168.1.232:10808
NO_PROXY=localhost,127.0.0.1,192.168.1.0/24
```

代理集中复用 Windows AI 节点现有的 v2rayN 出口，减少测试节点重复维护外网代理；`NO_PROXY` 保证本机和局域网服务不绕行代理，避免影响 SSH、节点间 API 和正式存储的内网访问。该配置作用于 Docker daemon 的对外访问，不代表业务容器已获得或需要同样的代理环境。

### 当前结论与下一阶段

基础测试环境已经完成，范围包括 Windows AI 开发节点、Hyper-V Debian 测试节点、固定局域网地址、SSH 管理、Docker/Compose、Docker daemon 代理和基础容器运行验证。`agent-wechat` 的部署、微信登录、联系人读取、聊天读取、消息读取和消息发送也已完成基础验证。该结论不表示附件能力、Gateway、Hermes、企业系统接口或业务主链路已经完成。

下一阶段按以下顺序推进：

1. 验证图片、Word、Excel、PPT、PDF、ZIP、中文文件名和连续多附件兼容性。
2. 完成 CF Gateway 的消息路由、安全隔离和控制面衔接设计。
3. 对接 Hermes、Worker Bridge 和 GPT-5.6 API。
4. 完成从微信消息到 Hermes 再返回原会话的端到端联调。

## Debian 服务状态与计划

| 服务 | 状态 | 计划职责 |
| --- | --- | --- |
| `agent-wechat` | 部署及基础读写已验证；附件待技术验证 | 微信消息收发、附件获取，与控制面隔离运行。 |
| `wechat-relay` | 待实施 | 标准化事件、去重、来源映射和结果回传。 |
| 上下文服务 | 待实施 | AI 线程、最近消息和上下文快照。 |
| 任务中心 | 待实施 | 任务批次、队列、状态机、租约、重试和恢复。 |
| `CF_filebrowser-enterprise`（正式企业 File Service） | **已实现 / 待验证** | V1 Beta 核心服务端能力已实现；最终 Debian 部署、生产挂载、备份恢复和发布验收待验证。 |
| FileBridge / `filebrowser-agentctl` | **待集成** | 仅作为受控客户端调用稳定 File Service API，不直接访问正式存储。 |
| 数据库与队列 | 产品待定 | 保存权威业务状态与可恢复任务；具体产品和拓扑待选。 |
| 权限、日志与审计 | **待集成 / 待验证** | File Service 的持久化 Audit、Recorder、Token Audit 和查询 API 已实现；剩余业务 Audit Action 及全系统权限、日志、告警仍待集成、待验证。 |

### File Service 部署边界

- **已确定：** `CF_filebrowser-enterprise` 同时提供人员入口和受控 API，是唯一的正式企业 File Service；不得另行部署第二套 File Service。
- **已实现：** API Token hash-only、旧 Token/Key V5 migration 与失败关闭、一次性明文、Browse / Preview / Download 三权 UI、Archive / Extract 权限与文件系统安全、Share `configured` / `effective` capability 服务端契约和密码 Share Token 绕过关闭。
- **已实现：** Persistent Audit Store、Audit V3 migration、request-scoped Audit Recorder、Token 创建/撤销/拒绝 Audit、管理员 `GET /api/audit`、过滤、Cursor 和日志脱敏。
- **待集成：** Share capability 前端 UI；登录、用户、权限、Share 管理 Audit；核心文件和 Archive Audit Action；WebDAV / OnlyOffice Audit Action；FileBridge、Gateway 与 Hermes 对接。
- **待验证：** 最终 Debian 部署、重启恢复、生产存储挂载、备份与回滚、V1 Beta 最终 tag/candidate。当前尚未正式生产部署。

FileBridge / `filebrowser-agentctl`、Gateway、Hermes 和 Skills 只能调用 File Service API，不得绕过文件权限、Token capability、Share capability 或 Audit。V1 Beta 完成后，Gateway/Hermes 只按稳定 File Service 契约集成。

## 计划中的 Windows AI 节点服务

| 服务 | 状态 | 计划职责 |
| --- | --- | --- |
| Hermes Worker Bridge | 待实施 | 心跳、领取任务、租约、隔离工作区、文件传输和结果回报。 |
| Hermes | 待实施 | 理解任务、规划并调用授权 Skills。 |
| GPT-5.6 API 接入 | 待实施 | 模型调用、超时、限流和错误归类；密钥由受控方式注入。 |
| Skills 运行环境 | 待实施 | 文档表格、浏览器、Windows 桌面及后续业务自动化。 |
| 节点监控 | 待实施 | 进程、心跳、队列租约、磁盘、网络和工具可用性上报。 |

## 部署前检查

以下条目是上线前必须满足的验收条件；当前 File Service 最终 Debian 部署仍为**待验证**，不表示这些条件已经在目标环境完成。

- 已确认 Debian、Windows AI 节点和云服务器的职责、网络路径、端口、鉴权与防火墙边界。
- 已确认机器人账号、允许群、发信人、岗位、Skill 和文件路径的最小权限矩阵。
- 存储容量、逻辑目录、临时区、保留周期、时钟同步、DNS 和内网传输已验证。
- 密钥与微信登录数据使用受控注入和备份方式，仓库、普通日志和任务正文中不存在真实值。
- 数据库 migration、备份恢复、队列持久化、Worker 离线和结果重发已有演练记录。
- **待验证：** 在目标环境执行旧 Token/Key V5 与 Audit V3 migration；迁移异常时服务失败关闭，不带不完整状态继续启动。
- **待验证：** 数据库不保存 API Token 明文，明文只在创建响应中展示一次，后续不能查询恢复。
- **待验证：** Share 授权只采用服务端返回的 `effective` capability，密码 Share Token 不能绕过密码或 capability 校验。
- **待验证：** 管理员 Audit 查询的权限、过滤、Cursor 和日志脱敏，并逐项核对业务 Audit Action 覆盖清单。
- **待验证：** File Service 在目标 Debian 环境中的部署、重启、备份恢复、存储挂载、健康检查和回滚演练。
- `agent-wechat` 附件兼容性、中文文件名、连续多附件、群聊/私聊和引用文字已经实测。
- 测试机器人、测试群和脱敏附件已准备，不直接用生产业务数据做首次联调。

## 健康检查

- 微信登录与收发是否正常，最近一次入站/出站事件是否可追踪。
- `wechat-relay` 是否持续接收、去重并写入权威存储。
- 数据库、队列、上下文、任务中心和 `CF_filebrowser-enterprise` File Service 是否可读写，migration、Audit Store、积压和错误率是否异常。
- Windows Worker 心跳、租约和执行槽是否正常，Hermes、模型 API 及必需 Skill 是否可调用。
- 测试文件能否在权限范围内下载、处理、回写和归档，哈希与关联是否一致。
- 端到端探针能否从测试会话创建任务并把结果返回同一窗口。

检查频率、阈值、告警渠道和自动恢复策略均为**待确认**。

## 微信收到但没有回复的排查链路

按同一个 `trace_id` / `task_id` 逐段检查，禁止只在 Windows 节点盲目重跑：

1. `agent-wechat` 是否收到事件，附件是否下载或明确失败。
2. `wechat-relay` 是否标准化、去重并正确识别机器人账号、`chat_id` 和 `sender_id`。
3. 权限判断是否拒绝，任务批次是否仍在等待补充或收口。
4. 任务是否已持久化、排队、等待确认、租约中或进入失败状态。
5. Worker 是否在线并领取任务，Hermes、模型 API 或 Skill 是否超时/报错。
6. 结果是否已写回 Debian，产物是否通过 `CF_filebrowser-enterprise` File Service API 登记。
7. 结果回传目标是否为原会话，发送是否失败、限流或仍在重试。

排查完成后在 Issue 保留过程，正式文档只补充可复用的当前结论。

## Hermes Worker 离线处理

- Debian 继续接收并持久化消息、附件、批次和任务，向用户返回“已接收/排队”或明确的延迟提示，不能丢任务。
- Worker 使用有期限租约；心跳超时后不立即认定业务未执行，先判断是否存在结果未知或部分成功。
- 只对确认安全、幂等的步骤自动重新派发；写操作结果未知时转人工核对，避免重复下单、改价或发送。
- Worker 恢复后从 Debian 权威状态续领任务，本地缓存不得覆盖较新的任务状态。

## 备份、升级与回滚

- 备份至少覆盖权威数据库、任务/审计记录、文件元数据、正式文件和必要的加密配置；微信登录数据单独受控，不混入普通备份。
- 定期做恢复演练并记录恢复点目标、恢复时间目标和校验结果；具体指标**待确认**。
- 升级采用版本化构建和 migration，先备份、再小范围验证、最后逐步切换；不在正式环境直接改代码。
- 回滚须同时考虑应用、schema、队列事件和文件版本；不可逆 migration 或业务写操作必须预先设计补偿方案。
- 生产运行不依赖 GitHub 持续在线；部署所需版本、依赖和回滚包应在受控环境可用。

## 日志与定位

- 入站事件生成 `trace_id`，任务批次和每个执行任务分别拥有稳定 ID；跨 Debian、Windows、模型、Skill、文件和微信回传时持续携带。
- 日志至少包含时间、组件、事件类型、状态、耗时、错误分类和关联 ID，不记录密钥、Cookie、微信登录数据或不必要的文件正文。
- 指标用于发现积压、失败率和离线状态，日志用于定位过程，审计记录用于回答“谁在何时以什么权限做了什么”。
- File Service 的 Audit Store、Recorder、Token Audit、查询过滤和日志脱敏**已实现**；剩余业务 Audit Action、跨组件关联、保留策略、告警阈值和仪表盘仍**待集成 / 待验证**。
