# 部署运维

> 状态日期：2026-09-03
>
> 本文只维护跨项目部署顺序、当前运行边界和正式入口。组件级 Compose、migration、登录脚本和 API 契约由对应精确 SHA 的组件仓库维护。

## 正式入口

| 主题 | 权威文档 |
| --- | --- |
| 跨项目部署顺序 | [Deployment Guide](./deployment/deployment-guide.md) |
| 故障与恢复 | [Recovery Runbook](./operations/recovery-runbook.md) |
| 生产验收 | [Production Validation Checklist](./validation/production-validation-checklist.md) |
| 当前能力 | [当前状态矩阵](./status/current-status.md) |
| 2026-09-03 证据 | [Production Closeout](./validation/records/2026-09-03-enterprise-runtime-production-closeout.md) |

## 当前生产范围

| 运行位置 | 组件 | 当前结论 |
| --- | --- | --- |
| CFserver | PostgreSQL、Gateway API、Poll/Dispatch/Delivery Worker、Runtime Controller | V2 与 P1 已生产交付，revision `20260823_04` |
| CFserver | external agent-wechat | forced-QR R2 行为已验证；实现尚未 main promotion |
| Windows AI 主机 | Hermes external runtime | 文本执行与一次 reboot reachability 恢复通过 |
| 未部署 | FileBrowser | V1 Beta 实现/自动化验证完成，生产部署待完成 |

## 跨项目部署顺序

1. 宿主、Docker、存储、时区和网络。
2. PostgreSQL。
3. Gateway core 与 migration/readiness。
4. Runtime Controller。
5. agent-wechat Bootstrap。
6. 关闭 Gate，执行 fresh QR，验证 auth/chats/messages。
7. Hermes reachability。
8. Gateway Poll/Dispatch/Delivery Worker Release。
9. Runtime status。
10. Production Validation Checklist。

## 场景边界

### CFserver reboot

- Gateway 核心与 PostgreSQL可自动恢复。
- agent-wechat 因 `restart: "no"` 保持停止，旧 Session 不恢复。
- automatic boot stop gate 未验证；不得假设 Poll/Delivery 已停止。
- Operator 必须先 status，再显式 stop Gate，然后 fresh QR 和 API 验证，最后恢复 Workers。

### Gateway-only deployment

- 不重建 agent-wechat。
- authenticated Session 保持。
- 不需要 fresh QR。
- 不得把 Gateway Compose 视为 agent-wechat/PostgreSQL 外部项目的所有者。

### agent-wechat restart/recreate

- 需要 fresh QR。
- 旧 Runtime 先归档但不复用。
- 先关闭 Gate，登录与 API 验证全部通过后再放流。

### AI host restart

- CFserver 与 agent-wechat 未重启时不需要 fresh QR。
- 必须重新核对 Hermes reachability、Controller、Dispatch 和 Queue。
- 单次恢复不等于 watchdog/告警/HA 全部交付。

### FileBrowser future deployment

- 必须独立形成 Candidate、migration、备份恢复、回滚、WebDAV/OnlyOffice 和权限审计验收。
- 未完成前不接入正式文件主链。

## Compose ownership

- Gateway 与 agent-wechat 是两个独立 Compose project。
- PostgreSQL 属于 Gateway 发布边界，agent-wechat 不得管理。
- 禁止跨项目 `docker compose down`。
- 禁止使用 `--remove-orphans` 清理外部 PostgreSQL、agent-wechat 或其他项目容器。
- 不在一个项目中复制、修改或替代另一个项目的 Compose。
- 不打印完整环境文件、渲染后的 Secret、Token 或连接串。

## 健康与证据

- HTTP liveness、database readiness、Runtime business health 和端到端回复分别检查。
- Controller 必须同时报告 ready 与 Token contract valid。
- Gateway 日志策略是 `64m x 10`；agent-wechat 是 `20m x 3`。
- 生产变更前保存版本、digest、revision、状态、日志和回滚目标。
- 一次性 Release/Archive 路径只写入带日期验证记录。

## 备份与恢复

- PostgreSQL 备份不等于 restore 已验证；实际 restore 演练仍是未完成门禁。
- agent-wechat Archive 是敏感证据，不是 active Session 恢复源。
- Gateway 回滚使用 previous immutable Release，不覆盖 active Release。
- FileBrowser 上线前必须单独完成数据库、配置、业务文件和审计恢复。
- 生产运行不持续依赖 GitHub 在线。
