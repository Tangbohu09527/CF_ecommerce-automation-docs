# 部署运维

> 状态日期：2026-08-13。本文只维护跨项目生产拓扑、运维入口、健康检查和安全边界；具体 Compose、登录、迁移和运行命令由对应项目仓库维护。

## 当前生产范围

| 运行位置 | 当前服务 | 状态 |
| --- | --- | --- |
| CFserver | `agent-wechat` | **已部署并实机验证** |
| CFserver | PostgreSQL、`gateway`、`wechat-worker`、`dispatch-worker`、`delivery-worker` | **已部署，五个服务均保持 healthy；授权 AI 闭环待验证** |
| Windows AI 主机 | Hermes Gateway 0.20.0 | **已部署，网络连通已验证；开机自启可靠性待收口** |
| File Service | `CF_filebrowser-enterprise` | 保持该项目既有状态，本次不变更 |

统一结论：

> 微信消息发现、持久化、Checkpoint、未授权拒绝和 Hermes 网络连通已实机验证；授权后的完整 AI 回复闭环仍待验证。

## 项目运维入口

以下链接指向各项目仓库的权威运维文档。本仓库不复制其中会随实现变化的命令。

| 运维主题 | 权威文档 |
| --- | --- |
| 微信入口生产部署 | [`CF_agent-wechat/docs/deployment/cfserver-production.md`](https://github.com/Tangbohu09527/CF_agent-wechat/blob/main/docs/deployment/cfserver-production.md) |
| 微信登录管理 | [`CF_agent-wechat/docs/login-management.md`](https://github.com/Tangbohu09527/CF_agent-wechat/blob/main/docs/login-management.md) |
| Gateway 生产部署 | [`CF_agent-gateway/docs/deployment/cfserver-production.md`](https://github.com/Tangbohu09527/CF_agent-gateway/blob/main/docs/deployment/cfserver-production.md) |
| Gateway 微信运行时 | [`CF_agent-gateway/docs/runtime/wechat-runtime.md`](https://github.com/Tangbohu09527/CF_agent-gateway/blob/main/docs/runtime/wechat-runtime.md) |
| 身份、权限与路由 | [`CF_agent-gateway/docs/security/identity-access-routing.md`](https://github.com/Tangbohu09527/CF_agent-gateway/blob/main/docs/security/identity-access-routing.md) |

## Compose 与网络边界

- CFserver 生产操作必须显式指定项目定义的 Compose 文件，不依赖当前目录下的隐式默认文件。
- `CF_agent-wechat` 当前使用 `docker/compose.cfserver.yaml`；Gateway 的具体文件名和命令以其生产部署文档为准。
- Gateway 与 `agent-wechat` 使用 `cf-internal` 容器网络；网络可达不替代 Token 鉴权。
- `agent-wechat` 生产设置 `ENABLE_VNC=0`，不使用 VNC、noVNC、x11vnc、websockify 或宿主桌面 X11。
- 不在本仓库记录真实局域网 IP、主机账户、端口凭证或本地绝对路径。

## 凭证与配置

- Token、API Key、数据库密码、微信登录数据和 Cookie 只保存在 root-only 文件、未提交的 `.env` 或受控密钥系统中。
- 不得把凭证写入普通 YAML、Compose 模板、Git、Issue、任务正文、普通日志或截图。
- 文档示例只使用 `<AGENT_WECHAT_TOKEN>`、`<HERMES_API_KEY>`、`<DATABASE_URL>`、`<CFSERVER_LAN_IP>` 和 `<AI_HOST_LAN_IP>` 等占位符。
- 配置变更需记录变更人、时间、目标环境、验证结果和回滚条件，但不得记录秘密值或 Token 指纹。

## 健康检查

### CFserver

- `agent-wechat` 登录状态正常，登录管理脚本可用；完全新设备 SSH 二维码扫码仍按待验证处理。
- PostgreSQL、`gateway`、`wechat-worker`、`dispatch-worker` 和 `delivery-worker` 均为 healthy。
- `wechat-worker` 持续轮询，Checkpoint 在新消息持久化后推进，没有重复处理历史基线。
- Admission 拒绝与允许结果符合当前身份和策略；拒绝消息不得调用 Hermes 或创建 Delivery Outbox。
- Response Persistence 与 Delivery Outbox 的积压、重试和最终状态可查询。
- Gateway 与 `agent-wechat` 的 `cf-internal` 网络和 Token 鉴权都通过。

### Windows AI 主机

- Hermes Gateway 0.20.0 进程和健康接口可用。
- CFserver 与 `dispatch-worker` 均能访问 Hermes。
- Windows 登录启动项存在不等于自启可靠；每次主机重启后必须单独确认 Hermes 已启动。
- 在自启问题收口前，人工启动与恢复步骤以 Hermes 运行环境的受控记录为准。

### 端到端

- 当前只可用未授权账号验证 Persist-first 与安全拒绝，不得借此宣称 AI 回复闭环完成。
- 身份、策略、Agent Profile 和会话绑定完成后，再按[当前状态矩阵](./status/current-status.md#下一阶段顺序)依次验证 Admission Allowed、Routing、Dispatch、响应持久化、Outbox 与微信回复。
- AI 执行成功与微信投递成功必须分别检查。

## 变更、备份与恢复

- PostgreSQL schema、数据、身份策略、Agent Profile、Conversation Binding、Gateway 配置或 Compose 变更前必须完成备份。
- 备份至少覆盖权威数据库、任务与审计记录、文件元数据、正式文件和必要的加密配置；微信登录数据单独受控，不混入普通备份或文档附件。
- 备份完成不等于可恢复；关键变更前必须确认恢复步骤、恢复目标和校验方式可用。
- 定期执行恢复演练，记录恢复点目标、恢复时间目标和校验结果；具体命令与介质策略由对应项目仓库维护。
- 升级和 migration 失败必须停止，不得带不完整状态继续运行。
- 回滚同时考虑应用版本、schema、队列/Outbox 状态和配置兼容性。
- 生产部署包、依赖和回滚材料应在受控环境可用，生产运行不得依赖 GitHub 持续在线。

## 排障顺序

微信收到消息但没有 AI 回复时，按同一脱敏关联 ID 自前向后检查：

1. `agent-wechat` 是否保持登录并能提供新消息。
2. `wechat-worker` 是否轮询、标准化并写入 Message Store。
3. Checkpoint 是否在持久化后正确推进。
4. Enterprise Identity 与 Source Identity Mapping 是否存在。
5. User Access Policy 与 Gateway Access Policy 是否允许。
6. Conversation-AgentProfile Binding 是否存在，V2 Routing 是否形成决定。
7. `dispatch-worker` 是否调用 Hermes，Hermes 是否返回结果。
8. Response Persistence 是否成功，Delivery Outbox 是否创建。
9. `delivery-worker` 是否领取并通过 `agent-wechat` 投递到原会话。

当前首次身份发现消息止于第 5 步之前的 Admission 拒绝，这是预期安全结果；没有 Hermes 调用和机器人回复。

## 日志与审计

- 日志记录时间、组件、事件类型、状态、耗时、错误分类和脱敏关联 ID。
- 不记录真实消息正文、微信稳定账号、群 ID、密钥、Cookie、数据库连接串或本地绝对路径。
- 日志用于定位过程，指标用于发现健康与积压，审计用于回答谁在何时以什么权限执行了什么。
- 正式文件操作的权限与审计由 `CF_filebrowser-enterprise` 负责；Gateway、Hermes 和 Skills 不得绕过 File Service。

当天生产状态和阻塞项见[微信运行时阶段收口记录](./status/2026-08-13-wechat-runtime-closeout.md)。
