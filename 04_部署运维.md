# 部署运维

> 当前状态：阶段 1 基础测试环境和 V1 Staging 微信文本消息 AI 闭环已完成。Debian 13 上的 `agent-wechat` Docker 与 Gateway Worker 已调用 Windows AI 主机 Hermes API，并把文本响应返回原微信会话；self message 防回环也已验证。图片 / 附件 / 文件处理、Context Builder、Task Queue、完整 Worker Bridge、Skill 和生产自动部署仍未完成。File Service V1 Beta 核心代码已实现并通过自动化测试，但尚未完成 Debian 实际部署验收。下文按组件标记状态；当前结论不代表生产上线或完整企业业务自动化。

## 测试运行环境基线（2026-07-22）

### 节点与网络

| 节点 | 系统与名称 | 网络配置 | 当前用途 |
| --- | --- | --- | --- |
| Windows AI 开发节点 | Windows 11；计算机名 `AItest` | `192.168.1.232/24` | Codex、Git/GitHub 和 v2rayN 局域网代理出口；V1 Staging Hermes API 调用已验证，完整 Worker Bridge、Skills 和生产运行仍待建设。 |
| Debian 测试节点 | Hyper-V 虚拟机名 `CF_wechat-lab`；主机名 `cf-wechat-lab`；Debian 13 Trixie | `eth0` 使用固定地址 `192.168.1.230`；通过 SSH 以用户 `linxi` 管理 | 承载 `agent-wechat` Docker、Gateway Worker 和阶段 1 Debian 侧服务验证；V1 Staging 微信文本闭环已完成。 |
| 正式存储服务器 | `CFserver` | `192.168.1.233` | 正式存储节点；当前只记录网络位置，不表示测试环境已经完成正式文件服务对接。 |

Debian 测试节点的内核版本为 `6.12.96+deb13-amd64`。Windows AI 节点与 Debian 测试节点处于同一 `192.168.1.0/24` 局域网；当前已通过内网完成 Gateway Worker 到 Windows Hermes API 的 Staging 文本调用，目标 Worker Bridge 的任务、租约和文件能力仍待建设。

### Hyper-V 关系

- `CF_wechat-lab` 运行在 Windows AI 节点 `AItest` 的 Hyper-V 中，使用 External Switch 网络模式。
- External Switch 使虚拟机以独立地址直接接入局域网，便于 SSH 管理，并与 Windows AI 节点及后续内网服务按真实节点关系联调。
- Windows 节点提供虚拟化宿主和 v2rayN 代理出口；Debian 虚拟机保持独立的操作系统、地址和服务边界。该宿主关系不改变 Debian 控制面与 Windows 执行节点的既定职责划分。

### Docker 环境

| 项目 | 已验证配置或结果 | 状态 |
| --- | --- | --- |
| Docker Engine | `Docker version 26.1.5+dfsg1, build a72d7cd` | 安装完成 |
| Docker Compose | `Docker Compose version 2.26.1-4` | 安装完成 |
| Docker daemon 代理 | HTTP、HTTPS 代理指向 Windows AI 节点的 v2rayN；本机及局域网地址直连 | 配置完成 |
| 容器运行验证 | `hello-world` | 测试通过 |

Docker daemon 的代理环境为：

```ini
HTTP_PROXY=http://192.168.1.232:10808
HTTPS_PROXY=http://192.168.1.232:10808
NO_PROXY=localhost,127.0.0.1,192.168.1.0/24
```

代理集中复用 Windows AI 节点现有的 v2rayN 出口，减少测试节点重复维护外网代理；`NO_PROXY` 保证本机和局域网服务不绕行代理，避免影响 SSH、节点间 API 和正式存储的内网访问。该配置作用于 Docker daemon 的对外访问，不代表业务容器已获得或需要同样的代理环境。

### 当前结论与下一阶段

基础测试环境已经完成，范围包括 Windows AI 开发节点、Hyper-V Debian 测试节点、固定局域网地址、SSH 管理、Docker/Compose、Docker daemon 代理和基础容器运行验证。`agent-wechat` 入口与 Gateway V1 Staging 微信文本 AI 闭环均已验证，包括 Hermes API 调用、原会话文本回复和 self message 防回环。该结论不表示图片、附件、文件处理、Skill、企业系统接口、生产高可用或完整企业业务自动化已经完成。

下一阶段按以下顺序推进：

1. 修复 Gateway V1 whole-room thread 与既定 `bot_account_id + group_chat_id + sender_id` 隔离设计的偏差，并复验同群不同员工。
2. 实现 Identity Management V1 管理能力。
3. 建设 Context Builder、Task Queue 和完整 Worker Bridge。
4. 验证图片、Word、Excel、PPT、PDF、ZIP、中文文件名和连续多附件，并建设受控文件链路。
5. 建设 Skill、企业系统接口和生产部署验收。

## V1 Staging 微信文本闭环部署拓扑（2026-08-04）

### 环境与版本

| 项目 | 已验证环境 |
| --- | --- |
| 操作系统 | Debian 13 |
| 微信入口 | `agent-wechat` Docker 容器 |
| Gateway | CF Agent Gateway Worker，运行在 Debian Staging |
| Hermes | Hermes API，运行在 Windows AI 主机 |
| 已验证范围 | 微信文本 Polling、Message Store、Identity / Permission Admission、Employee Workspace / AI Thread、Hermes Dispatch / Relay、微信回复和 self message 防回环 |

具体 Gateway 安装、运行命令和 Staging 操作记录归 `CF_agent-gateway` 仓库维护；`agent-wechat` Docker、VNC / noVNC 和微信登录操作归 `CF_agent-wechat` 仓库维护。本仓库只记录跨节点拓扑、总体状态和运维边界，避免复制会快速过期的命令。

完整链路、`chatId + text` 出站契约、`is_self=true` 过滤、线程偏差和 `393 passed` / `ruff` / `git diff --check` 证据见[Gateway V1 Staging 微信文本闭环验证记录](./status/gateway-wechat-staging-validation.md)。

## Debian 服务状态与计划

| 服务 | 状态 | 计划职责 |
| --- | --- | --- |
| `agent-wechat` | 部署及基础读写已验证；附件待技术验证 | 微信消息收发、附件获取，与控制面隔离运行。 |
| `CF_agent-gateway` | **V1 Staging 微信文本闭环已验证** | 已验证 Polling / Checkpoint、Message Store、Identity / Permission Admission、Employee Workspace / AI Thread、Hermes API 调用、文本结果回传和 self message 防回环；群聊线程实现偏差待修复。 |
| Context Builder | 待实施 | 最近消息选择和不可变上下文快照。 |
| 任务中心 | 待实施 | 任务批次、队列、状态机、租约、重试和恢复。 |
| `CF_filebrowser-enterprise`（正式企业 File Service） | **V1 Beta 核心代码冻结候选 / 部署待验收** | V1 Beta 核心代码已实现并通过自动化测试；Debian 实际部署、生产挂载、备份恢复、升级回滚和发布验收仍待完成。 |
| FileBridge / `filebrowser-agentctl` | **待集成** | 仅作为受控客户端调用稳定 File Service API，不直接访问正式存储。 |
| 数据库与队列 | 产品待定 | 保存权威业务状态与可恢复任务；具体产品和拓扑待选。 |
| 权限、日志与审计 | **File Service 已实现 / 全系统待集成** | File Service 的 Persistent Audit、查询及主要业务 Action 已实现并通过自动化测试；跨组件权限、关联、日志、告警和生产 retention 仍待集成、待验证。 |

### File Service 部署边界

- **已确定：** `CF_filebrowser-enterprise` 同时提供人员入口和受控 API，是唯一的正式企业 File Service；不得另行部署第二套 File Service。
- **代码状态：** V1 Beta 核心代码已实现并通过自动化测试。当前进入代码冻结候选、文档收口和 Debian 部署验收阶段。
- **已实现并测试：** Browse / Preview / Download 三权，Create / Modify / Delete / Replace 权限语义，Upload、Range Download、Thumbnail、Media、Archive Create / Extract、ACL、路径规范化以及软链接和特殊文件类型防护。
- **已实现并测试：** API Token hash-only、旧 Token / Key V5 migration 与失败关闭、明文仅创建响应一次性返回；Share `configuredCapabilities` / `effectiveCapabilities`、capability 前端 UI、Browse / Preview 派生、Upload Share 强制 Create、密码三态与 credential-active hash 精确绑定。
- **已实现并测试：** Persistent Audit Store、Audit V3 migration、Pending / Finalize / Recovery、RequestID、request-scoped Recorder、degraded 状态、管理员 Audit Query、过滤、分页、用于防止查询分页状态被篡改的签名 Cursor 校验和秘密泄漏防护；Action 已覆盖 Token、登录、用户、权限、Share、核心文件、Archive、WebDAV 和 OnlyOffice。
- **待集成：** FileBridge / `filebrowser-agentctl`、Gateway、Hermes 与 Skills 对稳定 File Service API 的自动化主线接入。
- **待验证：** Debian 实际部署、数据库 migration 真实环境、重启恢复、生产存储挂载、备份恢复、升级回滚、Docker / Compose、Nginx、TLS、systemd、开机自启、真实 WebDAV 客户端、真实 OnlyOffice 服务、V1 Beta Candidate、Git tag 和 GitHub Release。当前尚未正式生产部署。
- **未完成：** Audit WORM、外部防篡改 Audit 存档、Audit 管理前端 UI、完整生产 retention 验收和正式生产上线。

FileBridge / `filebrowser-agentctl`、Gateway、Hermes 和 Skills 只能调用稳定 File Service API，并接受用户权限、最小 API Token、Share effective capability 与 Persistent Audit 检查；不得直接访问正式存储或建设第二套 File Service。

## 计划中的 Windows AI 节点服务

| 服务 | 状态 | 计划职责 |
| --- | --- | --- |
| Hermes Worker Bridge | **部分路径已验证 / 完整能力待实施** | Gateway 到 Windows Hermes API 的文本调用已完成；心跳、领取任务、租约、隔离工作区、文件传输和恢复仍待建设。 |
| Hermes | **V1 Staging 文本 API 调用已验证** | 当前可处理受控文本消息；Skill 调用、文件上下文、生产运行和完整业务验收仍待实施。 |
| GPT-5.6 API 接入 | 待实施 | 模型调用、超时、限流和错误归类；密钥由受控方式注入。 |
| Skills 运行环境 | 待实施 | 文档表格、浏览器、Windows 桌面及后续业务自动化。 |
| 节点监控 | 待实施 | 进程、心跳、队列租约、磁盘、网络和工具可用性上报。 |

## 部署前检查

以下条目是上线前必须满足的验收条件；当前 File Service 最终 Debian 部署仍为**待验证**，不表示这些条件已经在目标环境完成。

- 已确认 Debian、Windows AI 节点和云服务器的职责、网络路径、端口、鉴权与防火墙边界。
- 已确认机器人账号、允许群、发信人、岗位、Skill 和文件路径的最小权限矩阵。
- 存储容量、逻辑目录、临时区、保留周期、时钟同步、DNS 和内网传输已验证。
- 密钥与微信登录数据使用受控注入和备份方式，仓库、普通日志和任务正文中不存在真实值。
- 数据库 migration、备份恢复、队列持久化、Worker 离线和结果重发已有演练记录。
- **上线门禁：** File Service 升级前必须完成备份及恢复验证，并保留可核对的恢复结果；没有可用恢复路径不得升级。
- **待验证：** 在目标环境执行用户 V5 migration 与 V2 -> V3 Audit migration；任何 mandatory migration 失败都必须阻止启动，不得带不完整状态继续运行。
- **待验证：** 数据库不保存 API Token 明文，明文只在创建响应中展示一次，后续不能查询恢复。
- **待验证：** Share 授权只采用服务端返回的 `effective` capability，密码 Share Token 不能绕过密码或 capability 校验。
- **首次部署验收：** 必须验证 Token 创建与撤销、Share capability 与密码生命周期、Persistent Audit 查询和恢复，以及 WebDAV / OnlyOffice 的权限与 Audit；代码测试通过不能替代该环境验收。
- **待验证：** 管理员 Audit 查询的权限、过滤、签名 Cursor 和日志脱敏，并在部署环境逐项核对已实现的业务 Audit Action。
- **待验证：** File Service 在目标 Debian 环境中的 Docker / Compose 运行、Nginx、TLS、systemd、开机自启、重启恢复、备份恢复、存储挂载、健康检查和回滚演练。
- `agent-wechat` 私聊文本、群聊文本、引用消息、文件消息和 ZIP 文件入口已经实测；图片、Office、PDF、中文文件名和连续多附件仍待验证。
- 测试机器人、测试群和脱敏附件已准备，不直接用生产业务数据做首次联调。

## 健康检查

- 微信登录与收发是否正常，最近一次入站/出站事件是否可追踪。
- `CF_agent-gateway` Polling / Checkpoint 是否持续推进、去重并写入 Message Store，Admission 结果是否符合当前策略。
- Polling 读取 `is_self=true` 消息时是否跳过 sink / admission / Hermes 并推进 Checkpoint，是否出现机器人回复回环。
- 数据库、队列、上下文、任务中心和 `CF_filebrowser-enterprise` File Service 是否可读写，migration、Audit Store、积压和错误率是否异常。
- Windows Worker 心跳、租约和执行槽是否正常，Hermes、模型 API 及必需 Skill 是否可调用。
- 测试文件能否在权限范围内下载、处理、回写和归档，哈希与关联是否一致。
- 端到端探针能否从测试会话创建任务并把结果返回同一窗口。
- 文本出站请求是否继续使用 `chatId + text` 契约，是否错误回退到 `content` 字段。

检查频率、阈值、告警渠道和自动恢复策略均为**待确认**。

## 微信收到但没有回复的排查链路

按同一个 `trace_id` / `task_id` 逐段检查，禁止只在 Windows 节点盲目重跑：

1. `agent-wechat` 是否收到事件，附件是否下载或明确失败。
2. Gateway Runtime / Polling 是否标准化、去重并正确识别机器人账号、`chat_id` 和 `sender_id`，Checkpoint 是否异常推进。
3. 权限判断是否拒绝，任务批次是否仍在等待补充或收口。
4. 任务是否已持久化、排队、等待确认、租约中或进入失败状态。
5. Worker 是否在线并领取任务，Hermes、模型 API 或 Skill 是否超时/报错。
6. 结果是否已写回 Debian，产物是否通过 `CF_filebrowser-enterprise` File Service API 登记。
7. 结果回传目标是否为原会话，发送是否失败、限流或仍在重试。

排查完成后在 Issue 保留过程，正式文档只补充可复用的当前结论。

## Hermes Worker 离线处理

- Debian 继续接收并持久化消息、附件、批次和任务，向用户返回“已接收/排队”或明确的延迟提示，不能丢任务。
- Worker 使用有期限租约；心跳超时后不立即认定业务未执行，先判断是否存在结果未知或部分成功。
- 只对确认安全、幂等的步骤自动重新派发；写操作结果未知时转人工核对，避免重复下单、改价或发送。
- Worker 恢复后从 Debian 权威状态续领任务，本地缓存不得覆盖较新的任务状态。

## 备份、升级与回滚

- 备份至少覆盖权威数据库、任务/审计记录、文件元数据、正式文件和必要的加密配置；微信登录数据单独受控，不混入普通备份。
- 定期做恢复演练并记录恢复点目标、恢复时间目标和校验结果；具体指标**待确认**。
- 升级采用版本化构建和 migration；升级前必须完成本次备份的恢复验证，再小范围验证并逐步切换，不在正式环境直接改代码。
- 回滚须同时考虑应用、schema、队列事件和文件版本；旧二进制不得直接读取已迁移数据库，不可逆 migration 或业务写操作必须预先设计兼容或补偿方案。
- 生产运行不依赖 GitHub 持续在线；部署所需版本、依赖和回滚包应在受控环境可用。

## 日志与定位

- 入站事件生成 `trace_id`，任务批次和每个执行任务分别拥有稳定 ID；跨 Debian、Windows、模型、Skill、文件和微信回传时持续携带。
- 日志至少包含时间、组件、事件类型、状态、耗时、错误分类和关联 ID，不记录密钥、Cookie、微信登录数据或不必要的文件正文。
- 指标用于发现积压、失败率和离线状态，日志用于定位过程，审计记录用于回答“谁在何时以什么权限做了什么”。
- File Service 的 Persistent Audit、Pending / Finalize / Recovery、查询过滤、签名 Cursor、日志脱敏和 Token、登录、用户、权限、Share、核心文件、Archive、WebDAV、OnlyOffice Action **已实现并通过自动化测试**；Audit WORM、外部防篡改存档、Audit 管理前端 UI、完整生产 retention、跨组件关联、告警阈值和仪表盘仍**待建设 / 待验证**。
