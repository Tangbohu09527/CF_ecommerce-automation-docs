# 部署运维

> 状态证据截止：2026-08-14
>
> 最近复核：2026-08-21
>
> 本文只维护跨项目生产拓扑、正式入口、健康检查期望和恢复边界；具体 Compose、登录、迁移和运行命令由目标发布版本及对应项目仓库维护。

## 正式生产入口

| 主题 | 正式文档 |
| --- | --- |
| 新机器部署、Docker、配置、升级与回滚 | [Deployment Guide](./deployment/deployment-guide.md) |
| Host、Container、Database 与 Application 时区 | [Timezone Policy](./deployment/timezone-policy.md) |
| Docker、微信、Hermes 与消息无回复恢复 | [Recovery Runbook](./operations/recovery-runbook.md) |
| 部署、升级与恢复验收 | [Production Validation Checklist](./validation/production-validation-checklist.md) |
| 当前能力、证据和未验证边界 | [当前状态矩阵](./status/current-status.md) |

精确发布输入，包括 commit 或 tag、镜像 digest、Compose 文件、服务名、migration、Secret 注入位置和健康地址，必须来自目标版本发布清单及组件仓库，不能从本文示例或历史文档推断。下文“健康检查期望”是上线验收和日常巡检的判定标准，不是实时监控快照；实时能力状态与运行证据分别以当前状态矩阵和验证记录为准。

## 当前生产范围

| 运行位置 | 当前服务 | 状态 |
| --- | --- | --- |
| CFserver | `agent-wechat` | **已部署并实机验证**；登录与文本收发可用，图片读取和提取已验证 |
| CFserver | PostgreSQL、`gateway`、`wechat-worker`、`dispatch-worker`、`delivery-worker` | **已部署，部分链路待验证**；私聊与 `group_sender` 群聊文本闭环已验证，媒体与完整恢复待完成 |
| Windows AI 主机 | Hermes Gateway 0.20.0 | **已部署，部分链路待验证**；文本执行已验证，开机自启、守护和告警待收口 |
| 以该项目当前文档为准 | `CF_filebrowser-enterprise` | **开发中**；本次不改变其既有验证结论 |

统一结论：

> 私聊和 group_sender 群聊的授权文本闭环已实机验证；媒体链路、引用上下文注入和完整宿主恢复仍待完成。

## 项目运维入口

以下链接指向各项目仓库的权威运维文档。本仓库不复制会随实现变化的底层命令。

| 运维主题 | 权威文档 |
| --- | --- |
| 微信入口生产部署 | [`CF_agent-wechat/docs/deployment/cfserver-production.md`](https://github.com/Tangbohu09527/CF_agent-wechat/blob/main/docs/deployment/cfserver-production.md) |
| 微信登录管理 | [`CF_agent-wechat/docs/login-management.md`](https://github.com/Tangbohu09527/CF_agent-wechat/blob/main/docs/login-management.md) |
| Gateway 生产部署 | [`CF_agent-gateway/docs/deployment/cfserver-production.md`](https://github.com/Tangbohu09527/CF_agent-gateway/blob/main/docs/deployment/cfserver-production.md) |
| Gateway 微信运行时 | [`CF_agent-gateway/docs/runtime/wechat-runtime.md`](https://github.com/Tangbohu09527/CF_agent-gateway/blob/main/docs/runtime/wechat-runtime.md) |
| 身份、权限与路由 | [`CF_agent-gateway/docs/security/identity-access-routing.md`](https://github.com/Tangbohu09527/CF_agent-gateway/blob/main/docs/security/identity-access-routing.md) |

媒体 Runtime 和 `uncertain` Dispatch 的正式运维入口尚未形成；完成后应由对应项目仓库提供权威文档，本仓库只增加索引。

## Compose 与网络边界

- CFserver 生产操作必须显式指定项目定义的 Compose 文件，不依赖当前目录下的隐式默认文件。
- `CF_agent-wechat` 当前使用 `docker/compose.cfserver.yaml`；Gateway 的具体文件名和命令以其生产部署文档为准。
- Gateway 与 `agent-wechat` 使用 `cf-internal` 容器网络；网络可达不能替代 Token 鉴权或 Admission。
- `agent-wechat` 生产设置 `ENABLE_VNC=0`，不使用 VNC、noVNC、x11vnc、websockify 或宿主桌面 X11。
- 不在本仓库记录真实局域网 IP、主机账户、端口凭证或本地绝对路径。

## 凭证与配置

- Token、API Key、数据库密码、微信登录数据和 Cookie 只保存在 root-only 文件、未提交的 `.env` 或受控密钥系统中。
- 不得把凭证写入普通 YAML、Compose 模板、Git、Issue、任务正文、普通日志或截图。
- 文档示例只使用 `<AGENT_WECHAT_TOKEN>`、`<HERMES_API_KEY>`、`<DATABASE_URL>`、`<CFSERVER_LAN_IP>` 和 `<AI_HOST_LAN_IP>` 等占位符。
- 配置变更需记录变更人、时间、目标环境、验证结果和回滚条件，但不得记录秘密值或 Token 指纹。

## 健康检查期望

### CFserver

- `agent-wechat` 登录状态正常，文本收发接口可用；完全新设备 SSH 二维码扫码仍按待验证处理。
- PostgreSQL、`gateway`、`wechat-worker`、`dispatch-worker` 和 `delivery-worker` 均为 healthy。
- `wechat-worker` 持续轮询，Message Store 写入后才推进 Checkpoint；Bot 自发消息不创建 AI 工作。
- Admission 的允许、拒绝和 `bot_not_mentioned` 结果符合身份、策略和真实 mention。
- Dispatch、Response Persistence、Delivery Outbox 与 Delivery Attempt 能按脱敏关联 ID 串联。
- Gateway 与 `agent-wechat` 的 `cf-internal` 网络和 Token 鉴权都通过。
- 媒体桥接入后，还需检查 Attachment 状态、私有存储文件完整性、保留期与清理积压。

### Windows AI 主机

- Hermes Gateway 0.20.0 进程和健康接口必须实际可用。
- Windows 登录启动项存在不等于进程已启动；每次主机重启后必须单独确认。
- 建立进程守护、主动健康探测和告警，覆盖不可达、延迟、错误率与 Dispatch 积压。
- 核对 Gateway 的 `external_profile_ref` 可解析到预期 Hermes 配置档案；Gateway 不负责创建档案。

### 端到端

- 分别验证私聊、群聊真实 `@`、群聊未 `@`、Bot 回复防回环和引用消息。
- 文本链路分别核对 Admission、Routing、Dispatch、Response、Outbox 与微信回执。
- 媒体链路必须额外核对来源二进制、Attachment、私有存储、Hermes 输入、Artifact READY、媒体 Outbox 和微信实际接收。
- AI 执行成功、响应持久化成功、Artifact READY 和微信投递成功是独立状态。

## Hermes 不可达与 uncertain

已验证的故障表现是：Hermes 服务不可达时，CFserver 连接超时，Dispatch 进入 `uncertain`，微信消息已经进入系统但没有 AI 回复；Hermes 人工启动后健康恢复。

当前曾完成一次带备份、证据核对和 Guard 的受控人工恢复，原 Dispatch 第二次执行成功。该记录证明可以审慎恢复单个事件，不代表人工直接修改数据库可以成为日常运维方式。

正式能力要求：

1. 提供受控管理命令或 API，按脱敏关联 ID 查询和恢复 `uncertain`。
2. 恢复前核对 Hermes、Gateway、Response 和 Delivery 侧证据，判断外部执行是否可能已发生。
3. 使用幂等 Guard 阻止重复业务副作用。
4. 记录操作者、原因、证据、决定和结果。
5. 结果不明时不自动盲重试，不把状态直接改成成功。

## 媒体运行边界

- PostgreSQL 只保存 Attachment、Artifact、完整性和生命周期元数据，不长期保存大文件 Base64。
- 入站二进制保存在 CFserver 的 Gateway 私有存储；下载完成并校验前不得标为可用。
- 出站下载只有在原子持久化与完整性验证后才能进入 `READY`；非 READY Artifact 不得创建媒体 Outbox。
- 普通聊天媒体按策略自动过期；需要正式归档的文件再通过 `CF_filebrowser-enterprise` 转存。
- 下载、保存、重试、清理和发送均需幂等与审计。
- 不通过桌面 UI、剪贴板或猜测 Windows 文件路径传输媒体。

## 变更、备份与恢复

- PostgreSQL schema、数据、身份策略、Profile 引用、Conversation Binding、Gateway 配置、Compose 或媒体存储策略变更前必须完成备份。
- 备份至少覆盖权威数据库、任务与审计记录、Attachment/Artifact 元数据、需要保留的私有二进制和必要的加密配置；微信登录数据单独受控。
- 备份完成不等于可恢复；关键变更前必须确认恢复步骤、恢复目标和校验方式。
- CFserver Gateway 应用服务 restart 后持久化恢复、AI Thread 与 Hermes 上下文复用已经验证；该操作未 recreate 容器，也未重启 PostgreSQL、CFserver、AI 主机或 Hermes。
- Gateway 容器 recreate、PostgreSQL 重启、CFserver 整机重启、AI 主机重启与 Hermes 自动恢复必须分别演练；低层级验证不能替代高层级验收。
- 升级和 migration 失败必须停止，不得带不完整状态继续运行。
- 生产运行不得依赖 GitHub 持续在线。

## 排障顺序

文本消息没有 AI 回复时，按同一脱敏关联 ID 自前向后检查：

1. `agent-wechat` 登录与消息读取。
2. `wechat-worker` 轮询、Message Store 和 Checkpoint。
3. Enterprise Identity、Source Identity Mapping 和两级策略。
4. Admission 结果；群聊检查真实 mention 和 `bot_not_mentioned`。
5. Conversation、Thread Policy、Agent Profile 外部引用和 V2 Routing。
6. Dispatch 状态；遇到 `uncertain` 立即转证据核对流程。
7. Hermes 进程、健康接口、配置档案和响应证据。
8. Response Persistence、Delivery Outbox 与 Delivery Attempt。
9. `delivery-worker` 到原会话的实际投递结果。

媒体消息在上述链路外继续检查：媒体 API 读取与校验、Attachment、私有存储、Hermes 媒体协议、`artifact_ref` 下载、Artifact `READY`、媒体 Outbox、`send_media` 和微信实际接收。

## 日志与审计

- 日志记录时间、组件、事件类型、状态、耗时、错误分类和脱敏关联 ID。
- 不记录真实消息正文、微信稳定账号、群 ID、密钥、Cookie、数据库连接串、文件哈希值或本地绝对路径。
- 日志用于定位过程，指标用于发现健康与积压，审计用于回答谁在何时以什么权限执行了什么。
- 正式文件操作的权限与审计由 `CF_filebrowser-enterprise` 负责；Gateway、Hermes 和 Skills 不得绕过 File Service。

最新生产证据和阻塞项见[2026-08-14 私聊、群聊及媒体验证记录](./status/2026-08-14-private-group-media-validation.md)。
