# 功能需求

> 状态日期：2026-08-14。本文件描述系统行为与验收边界；实现存在或服务 healthy 不等于对应范围已经实机验证。当前统一结论见[状态矩阵](./status/current-status.md)。

## 1. 微信私聊和群聊

- **状态：已部署并实机验证。** 私聊授权文本闭环与群聊明确 `@` 机器人的 `group_sender` 文本闭环均已完成实际微信回复；机器人回复不回环。
- **场景：** 员工向企业 Bot 微信私聊，或在允许的业务群中明确 `@` 当前机器人发起任务。
- **系统行为：** 识别机器人账号、物理会话、发信人、结构化 mention、消息类型和时间；获准处理后把结果返回同一会话。群聊没有真正 `@` 时仍持久化，但以 `bot_not_mentioned` 结束且不调用 AI。
- **当前边界：** 已验证 `group_sender`；`group_shared` 只存在于代码或设计，尚未实机验证。
- **验收标准：** 私聊与群聊分别覆盖接收、准入、执行、持久化和原会话投递；机器人回复不得再次触发 AI；同群不同员工不得串用未授权上下文。

## 2. 身份与允许范围

- **状态：已部署并实机验证。** 测试路径的 Enterprise Identity、Source Identity Mapping、User Access Policy、Gateway Access Policy 及允许/拒绝结果已实机验证；这不等于完整管理员功能和所有员工范围均已验收。
- **场景：** 不同员工在私聊、同一群或不同群中发起请求。
- **系统行为：** 以平台、Bot 账号和稳定来源发送者 ID 映射 Enterprise Identity，再依据 User Access Policy、Gateway Access Policy、会话和能力范围形成 Admission 结果。
- **异常处理：** 身份未映射或策略不允许时，消息仍进入 Message Store，但不得调用 Hermes、不得创建机器人回复，也不得泄露内部策略细节。
- **验收标准：** 允许与拒绝都能按脱敏关联 ID 和时间追踪；昵称、备注或群名不得成为授权依据。

## 3. Message Store 与 Checkpoint

- **状态：已部署并实机验证。** 3 秒轮询、17 个 Checkpoint、151 条历史消息基线跳过、新文本消息、引用消息和图片消息的来源事实持久化，以及 Checkpoint 推进已验证。
- **系统行为：** 进入 Gateway 的员工消息先持久化，再执行身份、权限和路由判断；持久化成功前不得推进该消息的处理结果状态。
- **首次启用：** 使用 `bootstrap_mode=latest` 建立当前高水位，避免把既有聊天历史重新处理为新任务。
- **验收标准：** 重复轮询不产生重复消息；历史基线不触发 AI；新消息可从来源记录追踪到 Message Store 与 Checkpoint。

## 4. 上下文、Workspace 与 AI Thread

- **状态：已部署并实机验证。** 同一员工 Workspace 复用、私聊与群聊 AI Thread 隔离、私聊与群聊 Hermes Thread 隔离，以及 CFserver Gateway 应用服务 restart 后复用原 AI Thread 和 Hermes 会话上下文均已验证。
- **系统行为：** Admission Allowed 后，为员工解析或创建 Workspace 与 AI Thread；当前 `private_sender` 与 `group_sender` 按物理会话和发送者边界隔离。
- **群聊要求：** 只有结构化事实表明发送者明确 `@` 当前机器人时才可进入群聊 AI 链路；不得从正文名称、引用或上一条消息继承 mention。
- **引用边界：** `reply_context` 已持久化，但被引用内容尚未自动注入 Hermes 请求。
- **验收标准：** 私聊绑定稳定；同群不同员工的个人消息、任务、附件和结果不串线；拒绝消息不创建执行关系；执行记录可追溯实际线程与策略。

## 5. 多消息与多附件任务批次

- **状态：开发中。** 行为边界已确定，聚合窗口、结束规则和当前生产验收待完成。
- **场景：** 员工先发说明，再连续发送补充文字和多个附件。
- **系统行为：** 将同一 AI Thread 内属于同一意图的连续消息和附件合并为任务批次，允许补充、显式确认或超时收口；批次与可执行任务使用不同标识。
- **异常处理：** 无法判断归属时询问用户；撤销、重复事件和并发发信人不得造成重复执行或上下文串线。
- **验收标准：** 连续消息、多附件、超时补充、撤销、重复投递和并发发信人测试中，批次归属和执行边界清楚。

## 6. V2 Routing、Agent Profile 与 Thread Policy

- **状态：已部署并实机验证。** 私聊 `private_sender` 与群聊 `group_sender` 路由已经形成实际 Hermes Dispatch；测试私聊和 AI 群均引用 Hermes 的 `default` 配置档案，但会话保持隔离。
- **Profile 行为：** Hermes 配置档案由 Hermes 自身创建和管理，每个档案拥有独立配置、技能和 `SOUL.md`。Gateway 不自动创建档案，只保存并选择 `external_profile_ref`。
- **Thread Policy：** Hermes 配置档案决定人格、配置、技能和 `SOUL.md`；Gateway Agent Profile 只通过 `external_profile_ref` 选择档案；Thread Policy 决定上下文由谁共享。三者不得混为一项设置。
- **异常处理：** Profile 外部引用缺失、绑定冲突或路由无可用目标时停止派发，保留明确状态，不用隐式默认绕过配置。
- **验收标准：** 每次决定可关联会话、Thread Policy、策略快照和外部 Profile 引用；`group_shared` 必须单独审批和实机验证。

## 7. Hermes 处理及 Skills

- **状态：已部署，部分链路待验证。** Hermes Gateway 0.20.0 已实际处理私聊和 `group_sender` 群聊文本消息并返回响应；媒体处理、Skills Runtime 和主机重启自动恢复尚未验证。
- **系统行为：** `dispatch-worker` 仅派发已通过 Admission 和 V2 Routing 的任务；Hermes 在授权范围内调用模型并按选定配置档案执行，后续选择获准 Skills 与工具。
- **异常处理：** Hermes 不可达会使 Dispatch 进入 `uncertain`；结果不明时不得盲目自动重试。需要证据核对、幂等 Guard 和正式恢复流程，不能把人工直接修改数据库作为常规运维。
- **验收标准：** 每次调用能关联身份、会话、Profile 引用、路由、输入、输出和错误；Hermes 具有开机自启、守护、健康监控、告警和受控恢复能力。

## 8. Response Persistence 与微信投递

- **状态：已部署并实机验证。** 私聊和 `group_sender` 群聊文本响应已先持久化，再创建 Delivery Outbox，并由 `delivery-worker` 通过 `agent-wechat` 实际送达原微信会话；机器人回复不回环。
- **系统行为：** Hermes 响应先持久化，再创建 Delivery Outbox 项；文本与未来媒体投递分别记录执行结果和投递结果。
- **异常处理：** 投递失败时保留 Outbox 状态和重试记录；“AI 执行成功”与“微信投递成功”必须分别记录。
- **当前边界：** 图片和文件的出站 Artifact 物化、`send_media` 及微信实际接收尚未完成。
- **验收标准：** 成功、可重试失败和最终失败均可追踪；不会发错会话、重复发送或因 CFserver Gateway 应用服务 restart 丢失结果。

## 9. 图片、文件与引用消息

- **状态：已部署，部分链路待验证。** 引用识别、`reply_context` 持久化及引用类型消息的文本回复闭环已验证；微信图片读取和提取已验证，媒体 AI 完整链路未接入。
- **引用边界：** 群聊引用但未真正 `@` 时按 `bot_not_mentioned` 安全拒绝。系统尚未把被引用内容自动注入 Hermes，因此不能声称 AI 已自动理解引用内容。
- **图片证据：** 已识别 `image/raw_type=3`，保存 Message Store 与 Raw Payload，从 `agent-wechat` media API 取得真实 JPEG 字节，并验证文件签名、大小和 SHA-256；未 `@` 的群图片没有误触发 Hermes。
- **待实现行为：** 创建 Attachment 元数据，将入站二进制持久化到 Gateway 私有存储，经准入后受控提交 Hermes；出站产物先物化为 READY Artifact，再进入 Delivery Outbox。
- **文件边界：** 常见办公文件、PDF、ZIP、其他常见压缩包、中文文件名、大小边界、连续多附件与文件收发仍待实机验证。
- **验收标准：** 任务与输出可追溯原消息、每个输入附件、完整性校验、生命周期和实际投递结果。

## 10. 文件保存与企业资料

- **状态：开发中。** `CF_filebrowser-enterprise` 的具体状态沿用其既有文档；Gateway/Hermes 文件接入仍待完成。
- **场景：** 附件可能只需直接保存到授权逻辑位置，也可能作为任务输入并在处理后归档。
- **系统行为：** 普通聊天媒体先保存在 Gateway 私有存储并按策略过期；有业务归档要求的文件再经 File Service 转存企业文件中心。正式文件操作必须通过用户权限、最小 Token capability、Share effective capability 和审计。
- **异常处理：** 路径不明、重名、权限不足或写入失败时不得覆盖正式文件；任何组件不得直连正式存储或建设第二套 File Service。
- **验收标准：** 原消息、输入文件、任务、过程产物、最终结果、版本、哈希、逻辑位置、权限决定和审计记录可关联。

## 11. Identity Management V1

- **状态：开发中。** 测试身份和来源映射已支撑私聊与群聊实测；完整管理员新增、移除、角色和白名单能力尚未验收。
- **场景：** 管理员维护可使用 Gateway 的员工身份、角色、微信来源账号和允许范围，并查看必要的员工微信展示信息与最近活动。
- **系统行为：** 来源账号稳定 ID 显式映射到 `enterprise_identity_id`。昵称、备注、头像和最近活跃时间只用于管理展示，不参与授权或身份自动合并。
- **异常处理：** 展示字段变化不得改变身份归属或权限；来源身份冲突、无权管理或允许范围变更失败时拒绝生效并保留可追踪结果。
- **验收标准：** 管理员能力按权限工作；来源 ID 映射稳定；展示字段变化不改变 `enterprise_identity_id` 或 Admission 结果。

## 12. 权限、确认与安全

- **状态：已部署，部分链路待验证。** 测试路径的未授权拒绝、私聊允许、群聊未 `@` 拒绝及真正 `@` 允许已经实机验证；完整岗位、群、Skill、文件和高风险确认矩阵尚待配置与验收。
- **系统行为：** 按用户、Gateway、会话、Agent Profile、Skill、业务动作和文件路径执行最小权限；改价、删除和批量写入等高风险动作需人工确认。
- **凭证边界：** Token、API Key 和数据库密码不得写入普通 YAML、仓库、任务正文或普通日志；只通过 root-only 文件、未提交的 `.env` 或受控密钥机制注入。
- **验收标准：** 越权被拒绝且不调用 AI；高风险动作有有效确认；日志和审计不包含凭证明文或真实聊天正文。

## 13. 可靠性与恢复

- **状态：已部署，部分链路待验证。** CFserver Gateway 应用服务 restart 后的持久化恢复、原 AI Thread 复用和原 Hermes 会话上下文复用已验证。
- **已知风险：** Windows 登录启动项不能证明 Hermes Gateway 进程已运行；Hermes 不可达时可能产生 `uncertain` Dispatch 和无回复消息。
- **尚未验证：** Gateway 容器 recreate、PostgreSQL 重启、CFserver 整机重启、AI 主机重启后的 Hermes 自动恢复。
- **系统行为：** CFserver 保存权威状态；Windows AI 主机离线时不丢已接收消息，恢复后按证据和持久化状态继续。
- **验收标准：** 具备健康监控与告警、正式 `uncertain` 管理能力、备份与恢复证据；不同级别的 restart/recreate/宿主重启分别验收。

## 14. 后续业务方向

- **状态：规划中。** Skills、独立 OCR、旺店通和 S6 集成尚未进入生产验收。
- **系统行为：** 按[当前状态矩阵中的 18 步](./status/current-status.md#下一阶段顺序)先完成可靠性、引用和媒体链路，再接入 FileBrowser、Skills 和业务系统。
- **验收标准：** 每类业务上线前具有经业务负责人确认的输入、输出、权限、异常、人工接管、幂等和回滚标准。
