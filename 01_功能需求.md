# 功能需求

> 状态日期：2026-08-13。本文件描述系统行为与验收边界；组件已经部署不等于对应端到端链路已经实机验证。当前统一结论见[状态矩阵](./status/current-status.md)。

## 1. 微信私聊和群聊

- **状态：已部署，部分链路待验证。** 私聊新消息发现、持久化、发送者和会话识别、Checkpoint 推进已实机验证；授权后的 AI 回复和群聊尚待验证。
- **场景：** 员工向企业 Bot 微信私聊，或在允许的业务群中明确 `@` 当前机器人发起任务。
- **系统行为：** 识别机器人账号、物理会话、发信人、结构化 mention、消息内容和时间；获准处理后把结果返回同一会话。
- **异常处理：** 未识别会话、消息缺失或回传失败时保留可追踪状态，不得静默丢失；群聊未明确 `@` 当前机器人时不得创建 AI 执行。
- **验收标准：** 私聊与群聊分别覆盖接收、准入、执行、持久化和原会话投递；机器人回复不得再次触发 AI；同群不同员工不得串用未授权上下文。

## 2. 身份与允许范围

- **状态：已部署，部分链路待验证。** 未授权账号安全拒绝已实机验证；测试账号的 Enterprise Identity、Source Identity Mapping 与允许路径尚未配置和验证。
- **场景：** 不同员工在私聊、同一群或不同群中发起请求。
- **系统行为：** 以平台、Bot 账号和稳定来源发送者 ID 映射 Enterprise Identity，再依据 User Access Policy、Gateway Access Policy、会话和能力范围形成 Admission 结果。
- **异常处理：** 身份未映射或策略不允许时，消息仍进入 Message Store，但不得调用 Hermes、不得创建机器人回复，也不得泄露内部策略细节。
- **验收标准：** 允许与拒绝都能按脱敏关联 ID 和时间追踪；昵称、备注或群名不得成为授权依据。

## 3. Message Store 与 Checkpoint

- **状态：已部署并实机验证。** 3 秒轮询、17 个 Checkpoint、151 条历史消息基线跳过、新私聊持久化和 Checkpoint 推进已验证。
- **系统行为：** 进入 Gateway 的员工消息先持久化，再执行身份、权限和路由判断；持久化成功前不得推进该消息的处理结果状态。
- **首次启用：** 使用 `bootstrap_mode=latest` 建立当前高水位，避免把既有聊天历史重新处理为新任务。
- **验收标准：** 重复轮询不产生重复消息；历史基线不触发 AI；新消息可从来源记录追踪到 Message Store 与 Checkpoint。

## 4. 上下文、Workspace 与 AI Thread

- **状态：开发中。** 数据模型和目标边界已确定；当前生产尚未为测试发送者建立身份、Workspace、AI Thread 与 Conversation-AgentProfile Binding。
- **系统行为：** 身份映射和 Admission Allowed 后，为员工解析或创建 Workspace 与 AI Thread，并按当前消息、可获得的文字引用、当前任务批次附件、同一员工在当前线程内的最近消息、少量必要群上下文和未完成关联任务状态选择必要上下文。
- **群聊要求：** 只有结构化事实表明发送者明确 `@` 当前机器人时才可进入群聊 AI 链路；不得从正文名称、引用或上一条消息继承 mention。
- **异常处理：** 引用内容取不到或上下文存在歧义时要求补充，不得猜测关键业务参数。
- **验收标准：** 私聊绑定稳定；同群不同员工的个人消息、任务、附件和结果不串线；拒绝消息不创建执行关系；执行记录能够追溯实际选入的上下文。

## 5. 多消息与多附件任务批次

- **状态：开发中。** 行为边界已确定，聚合窗口、结束规则和当前生产验收待完成。
- **场景：** 员工先发说明，再连续发送补充文字和多个附件。
- **系统行为：** 将同一 AI Thread 内属于同一意图的连续消息和附件合并为任务批次，允许补充、显式确认或超时收口；批次与可执行任务使用不同标识。
- **异常处理：** 无法判断归属时询问用户；撤销、重复事件和并发发信人不得造成重复执行或上下文串线。
- **验收标准：** 连续消息、多附件、超时补充、撤销、重复投递和并发发信人测试中，批次归属和执行边界清楚。

## 6. V2 Routing 与 Agent Profile

- **状态：已部署，部分链路待验证。** V2 Routing 组件已随 Gateway 栈部署；真实允许消息的路由尚未验证。
- **系统行为：** Admission Allowed 后，根据 Conversation-AgentProfile Binding、任务能力和权限选择 Agent Profile，再产生可追踪的路由决定。
- **异常处理：** Profile 缺失、绑定冲突或路由无可用目标时停止派发，保留明确状态，不使用隐式默认绕过配置。
- **验收标准：** 测试私聊只能路由到显式绑定的 Agent Profile；每次决定可关联输入、策略快照和目标 Provider。

## 7. Hermes 处理及 Skills

- **状态：已部署，部分链路待验证。** Hermes Gateway 0.20.0 网络连通已验证；真实授权消息的 Hermes Dispatch 与处理结果尚未验证。
- **系统行为：** `dispatch-worker` 仅派发已通过 Admission 和 V2 Routing 的任务；Hermes 在授权范围内调用模型，后续选择获准 Skills 与工具。
- **异常处理：** Hermes 不可达、模型超时、Skill 缺失或结果不确定时进入可恢复状态，不伪造成功结果；未经确认不得执行高风险动作。
- **验收标准：** 每次调用能关联身份、会话、Agent Profile、路由、输入、输出和错误；Windows AI 主机重启后 Hermes Gateway 能可靠恢复。

## 8. Response Persistence 与微信投递

- **状态：已部署，部分链路待验证。** `delivery-worker` 已运行且 healthy；本轮尚未产生获准 AI 响应，Response Persistence、Delivery Outbox 和微信真实回复均待验证。
- **系统行为：** Hermes 响应先持久化，再创建 Delivery Outbox 项；`delivery-worker` 通过 `agent-wechat` 投递到原 Bot 账号和原会话。
- **异常处理：** 投递失败时保留 Outbox 状态和重试记录；“AI 执行成功”与“微信投递成功”必须分别记录。
- **验收标准：** 成功、可重试失败和最终失败均可追踪；不会发错会话、重复发送或因进程重启丢失结果。

## 9. 图片、文件与引用消息

- **状态：开发中。** 较早入口验证不能替代当前生产链路验收；图片、文件和引用消息列入授权文本闭环后的生产测试。
- **场景：** 员工在私聊或群聊中发送图片、常见办公文件、PDF、ZIP、其他常见压缩包或文字引用，也可能连续发送多个附件。
- **系统行为：** 保存文件名、类型、大小、来源消息与受控引用；只有获准消息的附件才能进入任务上下文，并明确其用途是输入、参考资料或输出模板。
- **异常处理：** 下载失败、不支持、超限、损坏、内容不完整、与指令冲突或压缩包存在风险时暂停处理，保留消息并返回明确失败或确认状态。
- **验收标准：** 覆盖私聊、群聊、图片、常见办公文件、PDF、ZIP、中文文件名、大小边界、连续多附件、引用和失败重试；任务与输出能追溯原消息、每个输入文件和实际使用的附件。

## 10. 文件保存与企业资料

- **状态：开发中。** `CF_filebrowser-enterprise` 的具体状态沿用其既有文档；本轮没有改变其结论，Gateway/Hermes 文件接入仍待完成。
- **场景：** 附件可能只需直接保存到授权逻辑位置，也可能作为任务输入并在处理后归档。
- **系统行为：** 区分“直接保存”和“任务处理”意图，关联原件、过程产物与最终结果；正式文件操作必须通过 File Service API、用户权限、最小 Token capability、Share effective capability 和审计，FileBridge / `filebrowser-agentctl` 只作为受控客户端。
- **异常处理：** 路径不明、重名、权限不足或写入失败时不得覆盖正式文件；任何组件不得直连正式存储或建设第二套 File Service。
- **验收标准：** 原消息、输入文件、任务、过程产物、最终结果、版本、哈希、逻辑位置、权限决定和审计记录可关联。

## 11. Identity Management V1

- **状态：开发中。** 当前测试身份与映射是授权链路的立即配置项；完整管理员能力尚未验收。
- **场景：** 管理员维护可使用 Gateway 的员工身份、角色、微信来源账号和白名单，并查看员工微信侧展示信息与最近活动。
- **系统行为：** 支持管理员与员工角色，以及员工新增、移除和白名单管理；来源账号稳定 ID 显式映射到 `enterprise_identity_id`。昵称、备注、头像和最近活跃时间只用于管理展示，不参与授权或身份自动合并。
- **异常处理：** 展示字段变化不得改变身份归属或权限；来源身份冲突、无权管理或白名单变更失败时拒绝生效并保留可追踪结果。
- **验收标准：** 管理员能按权限完成员工和白名单管理；员工角色不能获得管理员能力；来源 ID 映射稳定，展示字段变化不改变 `enterprise_identity_id` 或 Admission 结果。

## 12. 权限、确认与安全

- **状态：开发中。** 未授权拒绝路径已实机验证；完整岗位、群、Skill、文件和高风险确认矩阵尚待配置与验收。
- **系统行为：** 按用户、Gateway、会话、Agent Profile、Skill、业务动作和文件路径执行最小权限；改价、删除和批量写入等高风险动作需人工确认。
- **凭证边界：** Token、API Key 和数据库密码不得写入普通 YAML、仓库、任务正文或普通日志；只通过 root-only 文件、未提交的 `.env` 或受控密钥机制注入。
- **验收标准：** 越权被拒绝且不调用 AI；高风险动作有有效确认；日志和审计不包含凭证明文或真实聊天正文。

## 13. 可靠性与恢复

- **状态：已部署，部分链路待验证。** Gateway 五服务 healthy；长期运行、进程重启、Outbox 重发、数据库恢复和 Hermes 自启仍需验收。
- **系统行为：** CFserver 保存权威状态；Windows AI 主机离线时不丢已接收消息，恢复后按持久化状态继续。
- **验收标准：** PostgreSQL 与配置变更前可备份和恢复；Worker 重启不重复业务副作用；Hermes 自启与人工恢复路径均有记录。

## 14. 后续业务方向

- **状态：规划中。** Skills、独立 OCR、旺店通和 S6 集成尚未进入生产验收。
- **系统行为：** 按[当前状态矩阵中的 18 步](./status/current-status.md#下一阶段顺序)执行，并在第 17、18 步接入 FileBrowser、企业资料、Skills 和高价值业务流程。
- **验收标准：** 每类业务上线前具有经业务负责人确认的输入、输出、权限、异常、人工接管、幂等和回滚标准。
