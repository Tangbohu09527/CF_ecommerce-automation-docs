# 功能需求

> 状态日期：2026-09-03
>
> 本文件描述系统应做什么以及当前验收边界。实现、CI、部署与生产验证必须分别判断。

## 1. 微信私聊和群聊

- **当前状态：** 私聊文本和真正结构化 `@` 机器人的群聊文本已完成生产闭环。
- **系统行为：** 员工从原微信会话发起请求；系统识别来源账号、物理会话、发送者、消息类型和 mention，并将结果返回同一会话。
- **安全行为：** 普通群消息未明确 `@` 时先持久化，再以 `bot_not_mentioned` 结束，不调用 AI。
- **验收边界：** 群聊真实回复不自动证明同群多个发送者的线程隔离；该场景仍需单独生产验收。

## 2. 身份与允许范围

- 稳定来源 ID 必须显式映射到 `enterprise_identity_id`；昵称、备注、头像和群名只用于展示。
- 未映射或未获准消息仍保留在 Message Store，但不得创建执行工作、调用 Hermes 或产生机器人回复。
- User Policy、Gateway Policy、会话类型、结构化 mention、能力和风险共同形成 Admission。
- 正式员工、岗位、群、Skill 和文件权限矩阵尚未完成分批验收。

## 3. Message、Checkpoint 与连续性

- 非 self 员工消息采用 Persist-first，持久化失败不得推进执行。
- 首次启用可用 `bootstrap_mode=latest` 建立高水位；恢复已有环境不得重新 bootstrap。
- forced-QR 后必须支持 Local ID 回退、Checkpoint generation、历史前缀跳过和实时后缀单次处理。
- self reply 必须受控跳过并推进 Checkpoint，避免回环。
- Message、Checkpoint 和 Queue 的动态数量只进入带日期验证记录。

## 4. Workspace、AI Thread 与 Thread Policy

- 一个企业身份拥有稳定 Employee Workspace；物理会话、AI Thread 与 Hermes Runtime Thread 必须分开。
- 私聊使用 `private_sender`。
- V2 群聊默认使用 `group_sender`，同群不同企业身份应进入不同 AI Thread。
- `group_shared` 默认不可用，只有形成明确决定、权限、迁移和生产验证后才能启用。
- 当前 V2 `group_sender` 已实现并通过自动化测试；同群多发送者的生产对照验收仍未完成。

## 5. Agent Profile 与路由

- Hermes 配置档案由 Hermes 管理；Gateway 只保存和选择 `external_profile_ref`。
- Thread Policy 决定上下文共享边界，Profile 决定所选外部配置；两者不得混用。
- Profile、策略或路由关键事实缺失时 fail closed，不得从显示名称或消息正文猜测。
- General AI Provider routing 尚未完成。

## 6. Context Runtime

- Gateway 应按已获准的企业身份和 AI Thread 提供 Timeline、最近消息、范围查询、搜索和版本化 Snapshot。
- Context 只投影已完成的持久化事实，不得跨线程、跨身份读取。
- Snapshot 是可重建派生数据，不替代 Message/Dispatch/Response 权威事实。
- 当前 Context Runtime 已实现并通过自动化测试；完整生产读取、搜索和 Snapshot 行为仍需逐项验收。
- RAG、长期 Memory 和企业知识库不属于当前已交付能力。

## 7. 多消息与多附件任务批次

- 同一 AI Thread 内属于同一意图的连续消息和附件可组成批次；批次与可执行工作使用不同标识。
- 结束条件可以是明确指令、显式确认或超时。
- 无法判断归属时必须询问，不得跨发送者或跨线程自动合并。
- 聚合窗口、撤销、补充和多附件生产验收尚未完成。

## 8. Hermes 与 Skills

- Dispatch Worker 只领取已持久化、已获准和已完成路由的工作。
- Hermes external runtime 负责 Agent 与模型执行，不覆盖 Gateway/PostgreSQL 权威状态。
- AI 主机重启后 Hermes reachability 曾恢复一次；长期 watchdog、告警、容量和高可用未收口。
- Skills Runtime、权限、人工确认、幂等和审计尚未接入。

## 9. Response、Delivery 与 Admin recovery

- Hermes 结果先形成 durable Response，再创建 Delivery Outbox；执行成功与微信投递成功分别记录。
- Delivery 失败不得重新调用 Hermes；已有 Response 时只恢复 Delivery。
- `uncertain` Dispatch 禁止自动盲重试。
- Gateway 已实现并测试 inspect、`retry-approved`、`mark-dead` 和证据支持的 `confirm-success`；每个动作的生产演练仍需单独留证。

## 10. 引用消息

- 引用结构和 `reply_context` 已持久化，引用类型文本可完成回复。
- 群聊引用不能替代结构化 `@`。
- 被引用正文自动注入 Hermes 尚未确认完成，不能声称 AI 已理解引用内容。

## 11. 图片与文件

- 当前已验证图片来源发现、JPEG 字节读取和完整性校验。
- 完整入站链要求 Attachment、Gateway 私有存储、生命周期、Admission 和受控 Hermes 媒体协议。
- 完整出站链要求 `artifact_ref`、受认证下载、原子物化、完整性校验、`READY` 和媒体 Delivery。
- 图片字节可读取不等于 Hermes 已支持看图；Gateway 代码中的部分 Artifact/媒体能力不等于系统级媒体闭环已交付。
- Word、Excel、PowerPoint、PDF、ZIP、中文文件名和多附件仍需逐类验收。

## 12. 企业文件服务

- `CF_filebrowser-enterprise` 是唯一正式 File Service。
- V1 Beta 核心实现和自动化验证已完成，但尚未在 CFserver 部署和生产验收。
- 正式文件访问必须经过用户权限、最小 Token capability、Share effective capability 和 Persistent Audit。
- Gateway 私有媒体存储只用于受控中转，不得演变为第二套企业文件系统。

## 13. 重启与恢复

### CFserver reboot

- Gateway 核心和 PostgreSQL 应恢复；agent-wechat 因 `restart: "no"` 保持停止。
- 不得假设 Poll/Delivery Gate 自动处于 stopped；fresh QR 前必须检查并显式停止。
- fresh QR、auth/chats/messages 验证通过后才能恢复 Workers。
- Archive 不能自动恢复为 active Session。

### AI host reboot

- agent-wechat 与微信 Session 未重启时通常不需要 fresh QR。
- 必须验证 Hermes reachability、Controller 和队列状态。
- 单次恢复证据不等于完整 watchdog 和高可用。

### Gateway-only deployment

- 不重建 agent-wechat 时保持已认证 Session，不需要 fresh QR。
- 该结论不得外推到 agent-wechat 自身重启。

## 14. 电商业务自动化

- 旺店通、S6、寄样、库存、订单、物流、链接和报表能力尚未接入生产。
- 每个业务集成必须定义接口、字段、身份、权限、确认、幂等、错误和回滚。
- 消息 Runtime、File Service 和 Skills 边界稳定后再按业务价值分批推进。
