# 功能需求

本文件只描述系统要做什么。除明确标为**已确定**的基线外，能力在实际验收前不得表述为已经稳定可用。

## 1. 微信私聊和群聊

- **状态：已确定；基础消息读取和发送已验证，Gateway 入站权限执行链已完成 Staging 真实联调，AI 回复回传及私聊/群聊端到端稳定性待技术验证。**
- **场景：** 员工向 AI 微信账号私聊，或在允许的业务群中向 AI 发起任务。
- **系统行为：** 识别机器人账号、会话、发信人、消息内容和时间，将处理结果返回同一聊天窗口。
- **异常处理：** 未识别会话、消息缺失或回传失败时记录原因并给出明确失败提示；不得静默丢失任务。
- **验收标准：** 私聊和群聊各完成一组消息接收、任务建立、状态跟踪和原会话回传测试。

## 2. 发信人和允许群识别

- **状态：已确定；联系人和聊天读取已验证，未知身份拒绝及给定 User Policy / Gateway Policy 的 `normal` 风险允许路径已完成 Staging 实测；完整身份管理、允许群和岗位权限矩阵待实施。**
- **场景：** 不同员工在同一群、不同群或私聊中发起请求。
- **系统行为：** 校验机器人账号、允许群和发信人身份，并依据岗位权限决定可用能力。
- **异常处理：** 不在白名单或无权操作时不执行任务，返回不泄露敏感信息的拒绝说明并留审计记录。
- **验收标准：** 允许与拒绝场景均可按机器人账号、`chat_id`、`sender_id` 和时间追踪。

## 3. 上下文与文字引用

- **状态：已确定；聊天读取已验证，上下文筛选、线程隔离及微信侧引用字段待技术验证。**
- **场景：** 员工连续补充信息、引用此前文字，或在群聊中多人交叉发言。
- **系统行为：** 优先组合当前消息、可获得的文字引用、当前任务附件、同一发信人的最近消息、少量必要群上下文和未完成关联任务状态。
- **异常处理：** 引用内容取不到或上下文存在歧义时要求补充，不猜测关键业务参数。
- **验收标准：** 同群不同发信人的任务上下文不会串线；机器人无需发送微信原生引用回复，但接收侧能读取实际可获得的文字引用。

## 4. 常见附件接收

- **状态：待技术验证。**
- **场景：** 私聊或群聊发送普通图片、Word、Excel、PowerPoint、PDF、ZIP 及其他常见压缩包。
- **系统行为：** 获取文件名、类型、大小和来源消息，完整保存附件并关联任务；保留中文文件名或建立可追溯的安全映射。
- **异常处理：** 下载失败、不支持、超限、损坏或压缩包存在风险时停止处理，保留消息记录并向原会话说明原因。
- **验收标准：** 必须实测上述格式、中文文件名、群聊/私聊和连续多附件；测试通过前不得声称 `agent-wechat` 已稳定支持。

## 5. 多消息与多附件任务批次

- **状态：已确定；聚合窗口和结束规则待确认。**
- **场景：** 员工先发说明，再连续发送补充文字和多个附件。
- **系统行为：** 将属于同一意图的连续消息和附件合并为一个任务批次，允许补充、确认完成或超时收口，避免每条消息机械创建独立任务。
- **异常处理：** 无法判断归属时询问用户；重复事件不得产生重复业务执行。
- **验收标准：** 连续消息、多附件、超时补充、重复投递和并发发信人测试中，批次归属清楚且不串线。

## 6. 附件作为任务输入

- **状态：已确定。**
- **场景：** 员工要求根据表格、文档、图片、PDF 或压缩包中的资料执行任务。
- **系统行为：** 在权限允许时把附件及其元数据纳入当前任务，明确哪些文件是输入、参考资料或预期输出模板。
- **异常处理：** 文件不可读、内容不完整或与指令冲突时暂停并请求确认，不基于缺失信息继续高风险操作。
- **验收标准：** 任务记录能追溯到原消息和每个输入文件，输出能说明实际使用了哪些附件。

## 7. 文件直接保存和处理后归档

- **状态：已确定；正式 File Service 核心服务端能力已实现，自动化保存和归档链路待集成，最终部署待验证。**
- **场景：** 附件可能只需保存到指定逻辑位置，也可能需要处理后再归档。
- **系统行为：** 区分“直接保存”和“任务处理”意图；保存原件、过程产物和最终结果的关联关系，并按授权逻辑路径归档。所有正式文件操作都经 `CF_filebrowser-enterprise` 的受控 File Service API；FileBridge / `filebrowser-agentctl` 只作为受控客户端。
- **异常处理：** 路径不明确、重名、权限不足或写入失败时不覆盖正式文件，提示用户选择或修正；客户端不得直接挂载、遍历或修改正式存储，也不得绕过 capability 或 Audit。
- **验收标准：** 两类流程均可从原消息追溯到最终文件位置、版本、哈希和操作记录；Gateway、Hermes 和受控客户端仅通过稳定 File Service 契约完成授权访问。File Service 已实现 Archive/Extract 安全能力不代表微信附件自动归档链路已经完成。

## 8. Hermes 处理及 Skill 调用

- **状态：已确定；Hermes Runtime 尚未接入，Gateway Hermes Adapter、Skill 执行链及具体 Skill 清单待建设。**
- **场景：** 任务需要理解自然语言、处理文档表格、查询系统或执行自动化操作。
- **系统行为：** Hermes 判断意图并在授权范围内选择 Skill；确定性步骤由脚本或工具完成，重要中间状态可回报。
- **异常处理：** 缺少 Skill、模型超时、工具失败或结果不确定时进入重试、等待确认或失败状态，不伪造成功结果。
- **验收标准：** 每次调用可关联任务、输入、Skill、执行结果和错误；高风险动作未经确认不得执行。

## 9. 结果回传原聊天窗口

- **状态：已确定；基础消息发送已验证，AI 结果端到端回传待实施。**
- **场景：** 任务成功、需要确认、失败或耗时较长。
- **系统行为：** 向原群聊或原私聊回传简洁状态、结果摘要及必要附件；群聊线程仍按发信人隔离。
- **异常处理：** 微信发送失败时保留待发送结果并重试，最终失败进入可查询的人工处理队列。
- **验收标准：** 成功、等待确认、业务失败和回传失败四类结果都能在任务记录中核对，且不会发错会话。

## 10. 权限、确认与失败提示

- **状态：已确定；未知身份拒绝和给定 `normal` 风险允许路径已完成 Staging 实测，完整岗位与能力权限矩阵待确认。**
- **场景：** 查询、写入、改价、删除、批量操作等风险不同。
- **系统行为：** 按用户、群、岗位、Skill 和文件路径校验权限；改价、删除、批量操作等高风险动作先展示关键参数并请求二次确认。
- **File Service 安全边界：** API Token 服务端只保存 hash，明文只在创建成功时返回并展示一次；Share 的 `configured` capability 是配置值，`effective` capability 是服务端计算的实际生效值，客户端不得自行放大权限。密码 Share Token 不得绕过密码或 `effective` capability 校验。
- **异常处理：** 权限不足、参数冲突、依赖离线或超时均返回可理解的状态和下一步，不暴露密钥或内部敏感细节。
- **验收标准：** 越权被阻止，高风险操作有确认凭证，失败可通过 `trace_id` / `task_id` 定位；Gateway、Hermes、FileBridge / `filebrowser-agentctl` 均不能绕过文件权限、Token capability、Share capability 或 Audit。Audit Store、Recorder 和部分 Action 已实现不等于所有业务操作均已审计，实际覆盖状态见[当前开发进度](./status/current-progress.md)。

## 11. Identity Management V1

- **状态：后续规划。**
- **场景：** 管理员维护可使用 Gateway 的员工身份、角色和微信来源账号，并查看员工微信侧展示信息与最近活动。
- **系统行为：** 支持管理员、员工角色；管理员新增员工、移除员工和管理白名单；管理员可以查看微信昵称、微信备注、微信头像和最近活跃时间。微信 ID 作为微信来源身份的稳定主键和映射键，进入 Gateway 后显式映射到 `enterprise_identity_id`；微信昵称和备注仅用于管理员展示，不参与授权或身份自动合并。
- **异常处理：** 微信昵称、备注或头像变化不得改变身份归属或权限；来源身份冲突、无权管理或白名单变更失败时拒绝生效并保留可追踪结果。
- **验收标准：** 管理员可以按权限完成员工新增、移除和白名单变更；员工角色不能获得管理员能力；微信 ID 映射保持稳定，展示字段变化不改变 `enterprise_identity_id` 或授权结果。

## 12. 后续业务方向

- **状态：后续规划。**
- **场景：** 寄样、库存、订单、物流、分销链接、商品资料、经营报表等高频业务逐步接入。
- **系统行为：** 按 [项目总纲的六个阶段](./00_项目总纲.md#六个建设阶段) 分批定义独立业务需求和验收标准，优先打通寄样闭环，再扩展查询、平台操作与报表。
- **异常处理：** 未完成业务规则、账号权限、数据口径和回滚验证的能力不得进入生产自动执行。
- **验收标准：** 每类业务上线前具有经业务负责人确认的输入、输出、权限、异常、人工接管和可回滚标准。
